# SDX Inspect — project memory

Read this first. It is the memory between sessions.

## What this is
- SDX Inspect: kitchen / stand inspection PWA for Sodexo Live! at Hard Rock Stadium. User: Joxel Da Silva (inspector). Requests arrive short, often in caps, usually with a screenshot; ship small versions fast, verify in the harness, report in plain words.
- React + Vite. Almost everything lives in `src/App.jsx` (~30k lines, single file) and `src/App.css`. `src/main.jsx` mounts `<App/>` and `<LanguageFab/>`. Service worker `public/sw.js` with cache name `sdx-inspect-vNNN` (bump every deploy). Firestore project `sodexoinspection`, venue id `default`.
- Deployed from `main` to https://joxelds.github.io/Claude . Users must close and reopen the app twice to get a new version.

## Deploy loop (every change)
1. Edit `src/App.jsx` / `src/App.css`.
2. `sed -i 's/sdx-inspect-vNNN/sdx-inspect-vNNN+1/' public/sw.js`
3. `npm run build` (from repo root; if `vite: not found`, run `npm ci`).
4. Run a harness script (below), then commit and `git push -u origin main`. Never push to any other branch. Never put model ids in commits, PR text or code. Commit trailer lines come from the session reminder.
5. If the container restarted, `node_modules` and the scratchpad are gone: `npm ci`, rebuild, recreate harness files. Check `git log origin/main` before pushing; cherry-pick onto latest main if the branch drifted.

## Harness (Playwright, local mode, no Firestore)
- Chromium `/opt/pw-browsers/chromium`, args `--no-sandbox --no-proxy-server`; route `https://app.local/Claude/*` to files in `dist/`.
- `localStorage.sdx_force_local = "1"` makes Firestore calls no-op. Seeds: `sdx_history_cache_default` (reports), `sdx_equip_registry_default` (registry mirror: items with `assetTag` + labelIndex entries with `name/brand/location`), `sdx_users_default`, `sdx_venue_settings_default`, `sdx_equip_setup/verified/confirmed_default`.
- Login badges: `448800` (inspector "App Reviewer"), `365582` (Joxel, global admin, use `window.dispatchEvent(new CustomEvent('sdx-nav',{detail:{page:'admin'}}))` to navigate). Crew user badge hash = sha256("sdx_badge_v2_"+badge).
- After sign-in click "on site". Navigate with `sdx-nav` (pages: `print_labels` = Stands & equipment, `kitchen_qr` = posters & licenses, `history`, `admin`, `crew`, `equipment_scanner`). `window.__sdxStands` exposes the loaded stand list.
- Portal (what teams see when scanning a stand QR): `?haccp=1&site=NAME&unit=114&loctype=Concession`. Ident → "This is my location" → form.
- In local mode `loadHistory` returns nothing; `loadStandList()` falls back to `sdx_history_cache_default`.
- `sdx_equip_reg_doc_default` = `{items, labelIndex, hidden}` seeds the registry in the online shape (used by v422-test.mjs).

## Data model (Firestore `venues/default/sharedMemory/*`, all writes `setDoc merge`)
- `equipmentRegistry`: `items{TAG}` (manual/walk units: assetTag,label,venueName,unit,floor,locType,location,brandName,standId), `labelIndex{TAG}` (units indexed from reports: name,brand,location,venueName,unit,standId), `hidden{uid}` (soft deletes; a unit is hidden if `hidden[uid] || hidden[TAG] || hidden["reg_"+TAG]`), `verified{standId}`, `confirmed{TAG}`, `setup{TAG}`, `cutoffMs/cutoffMode`. Doc was ~798 KB on 2026-09-14 (limit 1 MB) — needs compaction (drop index entries for hidden tags).
- `kitchenRegistry`: `items{id}` (stands added/edited by Joxel: site,unit,floor,license,locType), `hidden{id}`.
- `licenseRegistry`: `items{rowKey}` overlay on the static `LICENSE_REGISTRY` (INDEX 2026, 106 rows) — Joxel marks rows requested/active/added.
- `standNotices`: announcements shown in the portal for targeted stands.
- Rules: sharedMemory read public, write allowed (`notTooLarge` = ≤80 top-level keys); users delete forbidden (soft delete `removed:true`).
- All shared writes go through `writeSharedDoc` → local outbox `sdx_reg_outbox_default`, replayed by `flushOutbox()` on load; failures flash "Not saved yet".

## Stand identity (do not change casually)
- The license registry IS the stand list: `standSeeds()` = one stand per `licenseRows()` row; type from letter (C Concession, S Subcontractor, P Portable · "/ Sub" → Portable - Subcontractor, K Kitchen). Concession rows get the plain id.
- Stand id: `u:<normUnit>`; a second stand with a different name at the same unit gets `u:<unit>~<slug>` (`standIdIn`). Old report names never create stands. `sameStandName` (slug prefix) is identity; `looseStandName` (token overlap) is only for matching equipment → stand.
- `equipBelongsTo(item, stand)`: standId wins, then unit, then name (exact, then loose), then type, else the base stand. Unit-less items resolve by name (`standForUnitless`). Units that fit nothing show in "Unassigned equipment" with bulk assign.
- Registry record beats a report snapshot for label, brand, location, unit, venueName, locType, standId.
- Every `items[TAG]` write MUST carry `assetTag` (v422). Bare placement patches (move/retag) created tag-less records → ghost "Cooler" rows sharing a uid with the real unit → deleting the ghost deleted the real one. Load now fills tag/name from the index and dedupes by uid; `removeUnitVerify` matches uid+tag and offers ↩ Undo.

## Product decisions already made (don't relitigate)
- One QR per stand (poster). Equipment stickers were dropped in v400; posters stopped listing equipment in v419 (scan shows the live list). App version shown in menu footer + poster footer (v421); update banner at top. Per-unit QR was recommended against.
- Stands & equipment are one menu entry with two tabs (posters & licenses / equipment & verify walk).
- Verify walk (v392+): confirm / fix / remove / add per unit, mark stand verified; keep it working.
- Problem reports must be specific (v415/416): guided chips (which unit, what's wrong, which part, where) in portal, quick report and inspection form; out-of-range temps need a reason + action.
- Names, units, brands, locations, licenses stored and shown in UPPER CASE. Stand type badges everywhere; "NO LICENSE" / "REQUESTED" flags.
- Language switch (Google Translate) on every screen via `LanguageFab`; portal has native EN/ES strings.
- Crew boards (maintenance / Ecolab / cleaning) with per-stand picker and By Problem grouping; announcements to stands; invite links for roles.

## Training assets
- `docs/training/`: crew training deck (pptx) + EN/ES script. Video (3:44, silent, captions) built by scratchpad `train/record.mjs` (stage.html + phone iframe, Playwright recordVideo, ffmpeg-static → mp4); not in the repo. Crew roles are maintenance / cleaning / ecolab only — Pest Control is an issue type moved with "Not mine → move". The app's DevTools guard fires in small iframes: spoof `hasTouch` + `maxTouchPoints` in harnesses. Portal quick reports don't reach the crew board in local mode (Firestore only).
- Narrated build (v3): Higgsfield MCP (`higgfield`) — 16 narration clips `seed_audio` voice Holden `3c9d6053-6334-592c-8997-4e325286af3f`, 5 `seedance_2_5` clips (intro/outro/3 ambient). Egress here blocks `*.cloudfront.net` (result downloads) but allows PUT to the S3 presigned `media_upload` URLs; so the recording (`train/record.mjs`, chroma-green cards, `out/timeline.json`) is uploaded and the mix (colorkey + overlays + adelay/amix) runs in `sandbox_exec` (`train/gen_mix.py` builds the command), result confirmed as media `4bbef9a3-9ca2-4022-99ac-2e4884772901`, delivered as a Higgsfield link. Sandbox stdout truncates ~20 KB: check frames as tiny base64 JPEGs.
- Explainer "A game day with SDX Inspect" v1 (2:05, 1920×1080, no audio): prompt pack `docs/training/explainer-prompt-pack.md`; 3 character refs (gpt_image_2_5) + 14 Seedance 2.5 shots (`omni_reference`, ids in scratchpad `train/shots3.json`) + 8 app clips from `train/phone2.mjs` (inspection form Equipment step, hand-sink issue, board, card in process→after photo→fixed, portal temps, portal report, announce modal, follow-up). Composite `train/gen_explainer.py` (gzip+base64 into one `sandbox_exec` background command; xfade 0.6 s, app shots slowed 1.6×, others 1.25×; 360×720 rounded phone bottom-right; Montserrat EN 44 / gold ES 28 bottom-left; white Sodexo wordmark on title card, end card and as a corner mark). Result media `8e530da4-646d-40d9-84e3-2c79d6f754ea`. Check frames: keep sheets ≤ ~14 KB and split base64 with head/tail -c (stdout truncates ~20 KB); paste into a local file to view. Narrated version still to do. Credits 2,244 → 1,422 for this build.
- Explainer brief + screenshots: `docs/training/explainer-brief.md` (system summary, master prompt with 16 scenes, EN/ES lines, narration, screenshot index, short variants) and `docs/training/screens/` (50 PNGs: 34 phone 390×844@2×, 8 iPad 820×1180@2×, 8 desktop 1366×860@1.5×, plus `_all.png`), captured by scratchpad `train/shots.mjs` (phone2.mjs drivers + `page.screenshot`; poster = popup HTML re-rendered in a 794×1123 page; extra crew users 777002 cleaning / 777003 ecolab; svg/png MIME in the route or the header logo breaks). The SW update banner shows on iPad/desktop shots — cropped after capture.
- Explainer v2 (narrated, from the brief; 2:49, 1920×1080): UI motion rendered locally — `train/stage2.html` (device frames, kinetic EN/ES captions, flow diagram, cards; Montserrat woff2 fetched from Google Fonts into `train/fonts/`) driven by `train/record2.mjs` (Playwright recordVideo, `file://` screenshots from `docs/training/screens/`, scene lengths = narration length + 1.4 s, writes `timeline2.json`; recording starts ~0.7 s before page T0 → OFFSET 0.7). Narration: 16 seed_audio clips (Holden), one line per scene; no music model exists in Higgsfield, so the bed is synthesized (pad + pulse, side-chain ducked). Mix `train/gen_explainer2.py` → gzip+base64 `sandbox_exec` (restart:true; watch for unused filter outputs → 'asplit has an unconnected output'). Output media `ac8b1fff-a00f-4899-8189-fd60b7840621`.
- Explainer v3 (silent, learn-without-sound; 3:14, 1920×1080, no audio track): `train/stage3.html` + `train/record3.mjs` — Sora 700 display / Inter 500·600 body (woff2 in `train/fonts/`), Sodexo Live! lockup built in HTML (Inter italic wordmark + inline SVG swoosh/star; the SVG's own text uses a fallback font, so don't rasterise it), corner pill, step counter + EN + ES + 'how to' box per scene, 9–14 s scenes with a bottom progress bar, tap ripples, 3-D device entries, drawn flow connectors, three-device 'breathe'. Encoded locally (`stage3.mp4`, CRF 18), PUT straight to a `media_upload` slot (no sandbox needed). Output media `933f9b16-fc89-4070-8b02-61e562f4a24f`. Row badges go under the phones (y 905) so they don't hit the corner pill.
- Explainer v3.1 (silent; 3:15): Sodexo mark rebuilt to match the real logo — Nunito 900 italic 'sodexo' in #233D8F on a white plate, red SVG swoosh to a blue star (no 'Live!' inside the mark); all text Nunito 500–800 (`train/fonts/nunito*.woff2`). Scene 7 first phone was blank because the file was `16-portal-language.png` (real name `18-…`); `dev()` now logs `MISSING <src>` on img error — grep the record log. 'Laggy' feedback: Playwright screencast stalls on backdrop-filter / drop-shadow filter / blur(); replaced with box-shadow and gradient glow — check smoothness with `mpdecimate` unique-frame counts per 10 s window. Virtual-time frame capture (`record4.mjs`, CDP setVirtualTimePolicy) hangs on screenshot — don't retry that route. Output media `c8b81d1a-89ff-48d7-92c3-d004821a9808`.
- Explainer v3.2 (silent; 3:15): the ORIGINAL Sodexo logo — official vector fetched via the Higgsfield sandbox (Wikimedia `Special:FilePath&file=Sodexo_logo.svg`, 4,421 B; blocked from here, so print it as gzip+base64 from `sandbox_exec`), kept at `docs/training/sodexo-official.svg`, shown on a white plate (title/end cards + corner pill) in `train/stage3.html`. Output media `1159cce3-2dac-4454-9dd5-2fc38ae79ca9`.
- Explainer v4 (silent; 2:48, 1080p50, "dynamic, high quality, no lag"): UI stage recorded on pure green (`train/stage4.html`: transform-only motion — no opacity fades over green or they key as fringes; caption on an opaque navy panel; title/end card = 900 px panel at x 1300; `#flow svg` hidden when off) at 4× slow motion (`train/record4.mjs`: CDP `Animation.setPlaybackRate(1/4)` + waits ×4, then `setpts=PTS/4 -r 50`, CRF 14 → `stage4_ui.mp4`, PUT to a media_upload slot). Composite in `sandbox_exec` (`train/gen_explainer4.py` → `ex4_cmd.txt`): 14 Seedance shots looped/slowed 1.5×, blurred, vignetted, xfade at scene bounds from `timeline4.json` (OFFSET = ui duration − timeline total), `colorkey=0x00FF00:0.32:0.10,despill=green`, overlay, fades, libx264 CRF 17 preset fast (≈10 min; MUST run with background:true — a foreground nohup job dies with the sandbox). Output media `acac6a68-d8d0-4cdc-b756-5302a7d39fe9`.
- Explainer v5 (silent; 2:45, 1080p50) — "digital twin" look, no footage (Joxel: b-roll looked AI, keep the technological look, kitchens made digitally): `train/stage5.html` = stage4 + `#world` layer driven by `train/world.js` (isometric line-drawn kitchen: coolers, fryers+hood, hand sink, line, freezer, door with QR poster, labels; stadium concourse ring with 22 stand cubes; glow = second wide low-opacity stroke, draw-on via pathLength/dashoffset; per-scene presets `window.bg(name)` set camera (translate/scale transition), light objects `.ok/.warn/.hot`, add chips/markers, scan sweep, particles). `train/record5.mjs` = record4 with `BG[n]` per scene; encoded locally (`stage5.mp4`, CRF 16, 50 fps) and PUT straight to a media_upload slot — no sandbox. Output media `7bf24503-0358-46f9-aaef-96b693747bed`.
- v435: real push for fixes — `functions/index.js` (`notifyOnFix`, Firestore v2 trigger on `venues/{venueId}/sharedMemory/venueSettings`) diffs `followupStatus`, ignores replays older than 10 min, and multicasts one FCM message per newly resolved key to the tokens in `sharedMemory/pushTokens` (only roles inspector/admin/global_admin/location_manager), pruning dead tokens. Client: `public/firebase-messaging-sw.js` (compat SDK 10.14.1, background handler + notificationclick focus), `registerPushToken()` / `onPushMessage()` in `src/firebase.js` (called on sign-in for watcher roles; foreground messages go to the same bell via `fireNotification`). `VAPID_PUBLIC_KEY` in `src/firebase.js` is EMPTY — Joxel pastes the Web Push key and runs `npx firebase deploy --only functions,firestore:rules`; steps in `docs/PUSH-SETUP.md`. Blaze plan required (free tier covers this volume); iOS only delivers to a Home Screen PWA. Push delivery cannot be tested from this sandbox — the harness only proves the app still builds, registers without throwing when the key is empty (returns `not-configured`), and that the v434 in-app path still fires.
- v434: the inspector hears about fixes. ROOT CAUSE found: the `inspectorNotifications` collection was **missing from `firestore.rules`**, so the deny-all catch-all rejected every `saveInspectorNotification` write (live collection had 0 docs) — assignment notifications were dead too. Added a rules block for it (needs `firebase deploy --only firestore:rules` from Joxel's machine to take effect) and lowercased the two writers that wrote raw display names (the reader queries lowercase); crew updates now also go to every admin / global_admin, not just `f.inspector`. The feature itself does NOT depend on that deploy: crews already write the fix into `venueSettings.followupStatus`, which every device receives through the existing `onSnapshot`, so a new effect in the app shell (`FIXED_SEEN_KEY` = `sdx_fixed_seen_<venue>`) fires `fireNotification("✅ Fixed — STAND #unit", "category · by NAME")` for any resolved entry newer than the stored baseline; the baseline starts at `Date.now()` on first run so the backlog stays out of the bell. Follow-ups header gained a clickable `✅ N fixed today` chip opening `.fuFixedPanel`, rebuilt with `computeFollowups(history, {...vs, followupCleared:{}})` because the normal list drops cleared items. Cleaning crews are asked how it was resolved when they tap ✅ Fixed (`.crewHowRow`: "We cleaned it" / "It was already clean", required for that role only); the answer rides in `followupStatus[key].how` and shows in the alert and the Fixed panel. No push server exists, so nothing arrives with the app fully closed — say so.
- v433: the "Be specific" chip block is visually quieter (Joxel: "make it look a little less charged, it's going to confuse the user" — he first asked to delete it, then kept it). Same behaviour and same required rows everywhere; only `.spec*` in App.css changed: hairline border, `max-width: 620px` so it stops spanning a wide form, head 600-weight muted and reworded to "Tap what applies" / "Toca lo que aplica" (no 🎯, no "Be specific"), labels lighter, chips 1px / 600 weight with the selected state still solid navy, missing rows amber instead of a red block. NOTE: `.haccpCard` is a WHITE card — white-alpha overrides make the picker and its chips invisible there, so the portal override uses light greys (`#f7f8fa` / `#eceef3`).
- v432: the issue text on crew cards translates too. Picking a crew language now calls `applyLanguage(code)` from `LanguageFab.jsx` (so the page translator runs on the report content, Kreyòl = Google `ht`) instead of clearing the googtrans cookie, and every node whose words come from `T()` is marked `className="... notranslate" translate="no"` (header, lang row, tabs, stats, pick row, chips, empty states, stand/floor heads, item meta, move row, before/after heads, photo hints, actions, action box, status stamp, stand picker) so Google never re-translates the native strings or stand names. Left translatable on purpose: `.crewItemTitle`, `.crewItemDetail`, `.crewReportIssue`. Also fixed the two strings v431 missed (`⏰ overdue`, `✋ type by X`). Free translate APIs are blocked from this sandbox (googleapis returns a block page, MyMemory 403), so the widget path is the only verifiable one — the harness checks the cookie, the saved language and which nodes are protected; real translated content is confirmed on a phone.
- v431: crew boards speak English / Español / Kreyòl natively. `CrewBoardPage` holds `cl` (localStorage `sdx_crew_lang`, falling back to `sdx_portal_lang` then the `googtrans` cookie) and `T(en, es, ht)`, the same idea as the portal's `L(en, es)`; picking a language clears the googtrans cookie so Google never double-translates. Three pills + a `🌐 +` button (fires `sdx-open-lang` for the other 17 languages) sit above the tabs (`.crewLangRow/.crewLangBtn` in App.css). Board titles/nouns come from `boardTitle`/`boardNoun`, not `CREW_META`, so every header, menu item, tab, stat, filter, empty state, card button, action box and flash is translated. Report content (problem text, stand names, issue categories) stays as written.
- v430: the P122 Arepa Cart / Sub (NOS2334419) moved to unit 119 per the 2026 sheet note — `LICENSE_REGISTRY` row unit 122 → 119 (note "Moved from 122"), plus P119 keys in `HARD_ROCK_LICENSE_SEED`. Stand id went from `u:122~arepacartsub` to `u:119~arepacartsub`; checked first that no equipment / verified entry referenced the old id and that the `licenseRegistry` overlay had no 122 key. The full 2026 sheet (89 active + 17 requested/needed) was diffed against `LICENSE_REGISTRY` and matches row for row; only deliberate spelling fixes differ (Little Caesar, Benihana, Chef Creole, Donuts, Shula Burger, Mr. Chory).
- v429: PDF exports print through a hidden iframe (`printHtml(html, fallbackName)` next to `downloadBlob`) instead of `window.open` — Safari blocked the pop-up because `exportBulkPdf` awaits HACCP loads before opening it; fallback downloads the HTML. Follow-ups tab: "Select to export" → Excel / PDF / Word (`exportFollowupsDoc(items, "pdf"|"word")` in RecurringIssuesPanel, same rows as the Excel export, landscape table, photos in the PDF). Bulk export bar (`.bulkBar`) wraps on phones. Harness seed has no flagged issues, so the bulk issue picker can't be driven locally — test PDF printing via the follow-ups export.
- v428: explainer v5.2 (white logo, 8× recording; media `91f4832b-b620-4ff8-a7cf-6a95f4fcfe1b`) lives on the marketing page too: `public/marketing/video/sdx-explainer.mp4` (1280×720 30 fps CRF 26, 13 MB) + poster jpg, section `#video` after the hero. Playwright's Chromium can't decode H.264, so the harness only checks the element/poster. Request-access form: Department select removed (role tile sets it; 'Office / other' still asks).
- v427: crew / History dropdown menus anchor to the fixed header (a `position:relative` wrapper + the mobile `left:0` rule pushed them off-screen and widened the page); invite join with an existing badge now says "already exists as ROLE" instead of signing into the old account; Ecolab invites get the Ecolab department. Invite tokens live in `venueSettings.inviteTokens` (verified on the server via the Firestore REST API — readable from here).
- v424: Admin → Invite links has a QR button per role (modal with the code, Save PNG / Print / Copy link); uses the lazy `getQRCode()` loader.
- v423: typing a site name threw `invLicenseData is not defined` in `applySiteAutofill` (map only exists in the analytics panel); guarded.
- Promo (60 s, no voice): prompt pack in `docs/training/promo-prompt-pack.md`; 8 Seedance shots + 3 phone clips (`train/phone.mjs`, app fills top-left 390×780 of the 780×1560 recording → crop) composed by `train/gen_promo.py` (concat, rounded phone overlays, drawtext Montserrat, synthesized 100 bpm bed). Local ffmpeg-static has no drawtext: validate locally without text, render in the sandbox. Result media `c720486d-3fe9-478b-93b0-24cdaa2ecad6`.

## Open items
- Compact `equipmentRegistry` (size risk). Audit workbook was delivered 2026-09-14 (29 orphan units, 28 duplicate copies, 41 units missing brand/location, 43 stands without equipment, 22 stands without license, verify walk 0/116).
- Domain change: waiting for the domain name (needs CNAME, vite `base`, SW scope, Firebase authorized domains).
- App Store 3.2.0 resubmission on hold; Refero token exposed earlier — rotate; Higgsfield connector never got a callback URL.
