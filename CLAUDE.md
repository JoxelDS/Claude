# SDX Inspect — project memory

Read this first. It is the memory between sessions.

## What this is
- SDX Inspect: kitchen / stand inspection PWA for Sodexo Live! at Hard Rock Stadium. User: Joxel Da Silva (inspector). Requests arrive short, often in caps, usually with a screenshot; ship small versions fast, verify in the harness, report in plain words.
- React + Vite. Almost everything lives in `src/App.jsx` (~30k lines, single file) and `src/App.css`. `src/main.jsx` mounts `<App/>` and `<LanguageFab/>`. Service worker `public/sw.js` with cache name `sdx-inspect-vNNN` (bump every deploy). Firestore project `sodexoinspection`, venue id `default`.
- Deployed from `main` to https://joxelds.github.io/Claude . Users must close and reopen the app twice to get a new version.

## Deploy loop (every change)
1. Edit `src/App.jsx` / `src/App.css`.
2. `sed -i 's/sdx-inspect-vNNN/sdx-inspect-vNNN+1/' public/sw.js`
3. `npm run build` (from repo root; if `vite: not found`, run `npm ci`).
4. Run a harness script (below), then commit and `git push -u origin main`. Never push to any other branch. Never put model ids in commits, PR text or code. Commit trailer lines come from the session reminder.
5. If the container restarted, `node_modules` and the scratchpad are gone: `npm ci`, rebuild, recreate harness files. Check `git log origin/main` before pushing; cherry-pick onto latest main if the branch drifted.

## Harness (Playwright, local mode, no Firestore)
- Chromium `/opt/pw-browsers/chromium`, args `--no-sandbox --no-proxy-server`; route `https://app.local/Claude/*` to files in `dist/`.
- `localStorage.sdx_force_local = "1"` makes Firestore calls no-op. Seeds: `sdx_history_cache_default` (reports), `sdx_equip_registry_default` (registry mirror: items with `assetTag` + labelIndex entries with `name/brand/location`), `sdx_users_default`, `sdx_venue_settings_default`, `sdx_equip_setup/verified/confirmed_default`.
- Login badges: `448800` (inspector "App Reviewer"), `365582` (Joxel, global admin, use `window.dispatchEvent(new CustomEvent('sdx-nav',{detail:{page:'admin'}}))` to navigate). Crew user badge hash = sha256("sdx_badge_v2_"+badge).
- After sign-in click "on site". Navigate with `sdx-nav` (pages: `print_labels` = Stands & equipment, `kitchen_qr` = posters & licenses, `history`, `admin`, `crew`, `equipment_scanner`). `window.__sdxStands` exposes the loaded stand list.
- Portal (what teams see when scanning a stand QR): `?haccp=1&site=NAME&unit=114&loctype=Concession`. Ident → "This is my location" → form.
- In local mode `loadHistory` returns nothing; `loadStandList()` falls back to `sdx_history_cache_default`.
- `sdx_equip_reg_doc_default` = `{items, labelIndex, hidden}` seeds the registry in the online shape (used by v422-test.mjs).

## Data model (Firestore `venues/default/sharedMemory/*`, all writes `setDoc merge`)
- `equipmentRegistry`: `items{TAG}` (manual/walk units: assetTag,label,venueName,unit,floor,locType,location,brandName,standId), `labelIndex{TAG}` (units indexed from reports: name,brand,location,venueName,unit,standId), `hidden{uid}` (soft deletes; a unit is hidden if `hidden[uid] || hidden[TAG] || hidden["reg_"+TAG]`), `verified{standId}`, `confirmed{TAG}`, `setup{TAG}`, `cutoffMs/cutoffMode`. Doc was ~798 KB on 2026-09-14 (limit 1 MB) — needs compaction (drop index entries for hidden tags).
- `kitchenRegistry`: `items{id}` (stands added/edited by Joxel: site,unit,floor,license,locType), `hidden{id}`.
- `licenseRegistry`: `items{rowKey}` overlay on the static `LICENSE_REGISTRY` (INDEX 2026, 106 rows) — Joxel marks rows requested/active/added.
- `standNotices`: announcements shown in the portal for targeted stands.
- Rules: sharedMemory read public, write allowed (`notTooLarge` = ≤80 top-level keys); users delete forbidden (soft delete `removed:true`).
- All shared writes go through `writeSharedDoc` → local outbox `sdx_reg_outbox_default`, replayed by `flushOutbox()` on load; failures flash "Not saved yet".

## Stand identity (do not change casually)
- The license registry IS the stand list: `standSeeds()` = one stand per `licenseRows()` row; type from letter (C Concession, S Subcontractor, P Portable · "/ Sub" → Portable - Subcontractor, K Kitchen). Concession rows get the plain id.
- Stand id: `u:<normUnit>`; a second stand with a different name at the same unit gets `u:<unit>~<slug>` (`standIdIn`). Old report names never create stands. `sameStandName` (slug prefix) is identity; `looseStandName` (token overlap) is only for matching equipment → stand.
- `equipBelongsTo(item, stand)`: standId wins, then unit, then name (exact, then loose), then type, else the base stand. Unit-less items resolve by name (`standForUnitless`). Units that fit nothing show in "Unassigned equipment" with bulk assign.
- Registry record beats a report snapshot for label, brand, location, unit, venueName, locType, standId.
- Every `items[TAG]` write MUST carry `assetTag` (v422). Bare placement patches (move/retag) created tag-less records → ghost "Cooler" rows sharing a uid with the real unit → deleting the ghost deleted the real one. Load now fills tag/name from the index and dedupes by uid; `removeUnitVerify` matches uid+tag and offers ↩ Undo.

## Product decisions already made (don't relitigate)
- One QR per stand (poster). Equipment stickers were dropped in v400; posters stopped listing equipment in v419 (scan shows the live list). App version shown in menu footer + poster footer (v421); update banner at top. Per-unit QR was recommended against.
- Stands & equipment are one menu entry with two tabs (posters & licenses / equipment & verify walk).
- Verify walk (v392+): confirm / fix / remove / add per unit, mark stand verified; keep it working.
- Problem reports should be specific (v415/416): guided chips (which unit, what's wrong, which part, where) in portal, quick report and inspection form. Since v497 NOTHING blocks saving an inspection report (Joxel 2026-09-24: "do not make mandatory for them to fill up everything") — the v439/v454 hard photo + corrective gates are a soft "Before you save" list with 💾 Save anyway. The portal / quick-report photo + action gates are unchanged.
- Names, units, brands, locations, licenses stored and shown in UPPER CASE. Stand type badges everywhere; "NO LICENSE" / "REQUESTED" flags.
- Language switch (Google Translate) on every screen via `LanguageFab`; portal has native EN/ES strings.
- Crew boards (maintenance / Ecolab / cleaning) with per-stand picker and By Problem grouping; announcements to stands; invite links for roles.

## Training assets
- `docs/training/`: crew training deck (pptx) + EN/ES script. Video (3:44, silent, captions) built by scratchpad `train/record.mjs` (stage.html + phone iframe, Playwright recordVideo, ffmpeg-static → mp4); not in the repo. Crew roles are maintenance / cleaning / ecolab only — Pest Control is an issue type moved with "Not mine → move". The app's DevTools guard fires in small iframes: spoof `hasTouch` + `maxTouchPoints` in harnesses. Portal quick reports don't reach the crew board in local mode (Firestore only).
- Narrated build (v3): Higgsfield MCP (`higgfield`) — 16 narration clips `seed_audio` voice Holden `3c9d6053-6334-592c-8997-4e325286af3f`, 5 `seedance_2_5` clips (intro/outro/3 ambient). Egress here blocks `*.cloudfront.net` (result downloads) but allows PUT to the S3 presigned `media_upload` URLs; so the recording (`train/record.mjs`, chroma-green cards, `out/timeline.json`) is uploaded and the mix (colorkey + overlays + adelay/amix) runs in `sandbox_exec` (`train/gen_mix.py` builds the command), result confirmed as media `4bbef9a3-9ca2-4022-99ac-2e4884772901`, delivered as a Higgsfield link. Sandbox stdout truncates ~20 KB: check frames as tiny base64 JPEGs.
- Explainer "A game day with SDX Inspect" v1 (2:05, 1920×1080, no audio): prompt pack `docs/training/explainer-prompt-pack.md`; 3 character refs (gpt_image_2_5) + 14 Seedance 2.5 shots (`omni_reference`, ids in scratchpad `train/shots3.json`) + 8 app clips from `train/phone2.mjs` (inspection form Equipment step, hand-sink issue, board, card in process→after photo→fixed, portal temps, portal report, announce modal, follow-up). Composite `train/gen_explainer.py` (gzip+base64 into one `sandbox_exec` background command; xfade 0.6 s, app shots slowed 1.6×, others 1.25×; 360×720 rounded phone bottom-right; Montserrat EN 44 / gold ES 28 bottom-left; white Sodexo wordmark on title card, end card and as a corner mark). Result media `8e530da4-646d-40d9-84e3-2c79d6f754ea`. Check frames: keep sheets ≤ ~14 KB and split base64 with head/tail -c (stdout truncates ~20 KB); paste into a local file to view. Narrated version still to do. Credits 2,244 → 1,422 for this build.
- Explainer brief + screenshots: `docs/training/explainer-brief.md` (system summary, master prompt with 16 scenes, EN/ES lines, narration, screenshot index, short variants) and `docs/training/screens/` (50 PNGs: 34 phone 390×844@2×, 8 iPad 820×1180@2×, 8 desktop 1366×860@1.5×, plus `_all.png`), captured by scratchpad `train/shots.mjs` (phone2.mjs drivers + `page.screenshot`; poster = popup HTML re-rendered in a 794×1123 page; extra crew users 777002 cleaning / 777003 ecolab; svg/png MIME in the route or the header logo breaks). The SW update banner shows on iPad/desktop shots — cropped after capture.
- Explainer v2 (narrated, from the brief; 2:49, 1920×1080): UI motion rendered locally — `train/stage2.html` (device frames, kinetic EN/ES captions, flow diagram, cards; Montserrat woff2 fetched from Google Fonts into `train/fonts/`) driven by `train/record2.mjs` (Playwright recordVideo, `file://` screenshots from `docs/training/screens/`, scene lengths = narration length + 1.4 s, writes `timeline2.json`; recording starts ~0.7 s before page T0 → OFFSET 0.7). Narration: 16 seed_audio clips (Holden), one line per scene; no music model exists in Higgsfield, so the bed is synthesized (pad + pulse, side-chain ducked). Mix `train/gen_explainer2.py` → gzip+base64 `sandbox_exec` (restart:true; watch for unused filter outputs → 'asplit has an unconnected output'). Output media `ac8b1fff-a00f-4899-8189-fd60b7840621`.
- Explainer v3 (silent, learn-without-sound; 3:14, 1920×1080, no audio track): `train/stage3.html` + `train/record3.mjs` — Sora 700 display / Inter 500·600 body (woff2 in `train/fonts/`), Sodexo Live! lockup built in HTML (Inter italic wordmark + inline SVG swoosh/star; the SVG's own text uses a fallback font, so don't rasterise it), corner pill, step counter + EN + ES + 'how to' box per scene, 9–14 s scenes with a bottom progress bar, tap ripples, 3-D device entries, drawn flow connectors, three-device 'breathe'. Encoded locally (`stage3.mp4`, CRF 18), PUT straight to a `media_upload` slot (no sandbox needed). Output media `933f9b16-fc89-4070-8b02-61e562f4a24f`. Row badges go under the phones (y 905) so they don't hit the corner pill.
- Explainer v3.1 (silent; 3:15): Sodexo mark rebuilt to match the real logo — Nunito 900 italic 'sodexo' in #233D8F on a white plate, red SVG swoosh to a blue star (no 'Live!' inside the mark); all text Nunito 500–800 (`train/fonts/nunito*.woff2`). Scene 7 first phone was blank because the file was `16-portal-language.png` (real name `18-…`); `dev()` now logs `MISSING <src>` on img error — grep the record log. 'Laggy' feedback: Playwright screencast stalls on backdrop-filter / drop-shadow filter / blur(); replaced with box-shadow and gradient glow — check smoothness with `mpdecimate` unique-frame counts per 10 s window. Virtual-time frame capture (`record4.mjs`, CDP setVirtualTimePolicy) hangs on screenshot — don't retry that route. Output media `c8b81d1a-89ff-48d7-92c3-d004821a9808`.
- Explainer v3.2 (silent; 3:15): the ORIGINAL Sodexo logo — official vector fetched via the Higgsfield sandbox (Wikimedia `Special:FilePath&file=Sodexo_logo.svg`, 4,421 B; blocked from here, so print it as gzip+base64 from `sandbox_exec`), kept at `docs/training/sodexo-official.svg`, shown on a white plate (title/end cards + corner pill) in `train/stage3.html`. Output media `1159cce3-2dac-4454-9dd5-2fc38ae79ca9`.
- Explainer v4 (silent; 2:48, 1080p50, "dynamic, high quality, no lag"): UI stage recorded on pure green (`train/stage4.html`: transform-only motion — no opacity fades over green or they key as fringes; caption on an opaque navy panel; title/end card = 900 px panel at x 1300; `#flow svg` hidden when off) at 4× slow motion (`train/record4.mjs`: CDP `Animation.setPlaybackRate(1/4)` + waits ×4, then `setpts=PTS/4 -r 50`, CRF 14 → `stage4_ui.mp4`, PUT to a media_upload slot). Composite in `sandbox_exec` (`train/gen_explainer4.py` → `ex4_cmd.txt`): 14 Seedance shots looped/slowed 1.5×, blurred, vignetted, xfade at scene bounds from `timeline4.json` (OFFSET = ui duration − timeline total), `colorkey=0x00FF00:0.32:0.10,despill=green`, overlay, fades, libx264 CRF 17 preset fast (≈10 min; MUST run with background:true — a foreground nohup job dies with the sandbox). Output media `acac6a68-d8d0-4cdc-b756-5302a7d39fe9`.
- Explainer v5 (silent; 2:45, 1080p50) — "digital twin" look, no footage (Joxel: b-roll looked AI, keep the technological look, kitchens made digitally): `train/stage5.html` = stage4 + `#world` layer driven by `train/world.js` (isometric line-drawn kitchen: coolers, fryers+hood, hand sink, line, freezer, door with QR poster, labels; stadium concourse ring with 22 stand cubes; glow = second wide low-opacity stroke, draw-on via pathLength/dashoffset; per-scene presets `window.bg(name)` set camera (translate/scale transition), light objects `.ok/.warn/.hot`, add chips/markers, scan sweep, particles). `train/record5.mjs` = record4 with `BG[n]` per scene; encoded locally (`stage5.mp4`, CRF 16, 50 fps) and PUT straight to a media_upload slot — no sandbox. Output media `7bf24503-0358-46f9-aaef-96b693747bed`.
- Walk-in cooler poster (2026-09-18, Joxel: "can you make our own of this" — a third-party shelf-order infographic): `docs/training/cooler-poster.html` (self-contained: Nunito woff2 as data URIs, official Sodexo SVG inline, everything drawn in SVG — five shelves Ready to Eat 135°F → Chicken 165°F, veg rack, 6" floor callout, EN + ES, footer rules cover/label · FIFO · raw below RTE), rendered by scratchpad `train/poster.mjs` to `cooler-poster.png` (2200×3400) and `cooler-poster.pdf` (11×17 in, one page). Regenerate with `python3` edits to the HTML + `node train/poster.mjs`. Layout lives in one `<svg class="scene" viewBox="-150 0 1360 1260">` — pills sit at x −88, right rack posts at 678–1182, shelves at y 170+i·215, floor at 1200. Slide cuts (Joxel: "something bigger that i can put here" — his 16:9 'Cooler Storage and Labeling' deck slide): `cooler-slide.html/.png` (1760×880 @2×, TRANSPARENT, no title/footer: the scene at 900 px wide on the left + a right column with the lead line, four red temperature pills and four rule cards — built by extracting the poster's `<svg class="scene">` + font CSS) and `cooler-scene.png` (scene only, 1300×1200 @2×, transparent). Rendered by scratchpad `train/slide.mjs` (`omitBackground: true`); the fit mock must load the PNG from a `file://` html, not `setContent`.
- pH scale slide (2026-09-18, Joxel: "can you give me this ph" — a third-party universal-indicator chart): `docs/training/ph-scale.html/.png` (1760×880 @2×, transparent, no title — for his deck): 15 coloured blocks 0–14, lollipop icons (emoji) with EN/ES labels alternating high/low, ACID / NEUTRAL / ALKALI arrows, and a "Why it matters in the kitchen" box (sanitizer pH, pH 4.6 rule, test strips). Same Nunito data-URI fonts pulled from `cooler-poster.html`.
- v497: nothing is mandatory to save a report, and Samantha's PANTRY #307 report was saved for her. Joxel: "Do not make mandatory for them to fill up everything and also please save the one that Samantha is doing". (a) `onTransform(skip)`: `skip === true` only (the header button passed the click event, which silently skipped the old "Incomplete Sections" check — that dead block is deleted); the BEFORE-photo/action check on NO rows and the open-problems (`caMissing`) check now build ONE list → `preSubmit: { soft: true, incomplete }` — ONE modal "💾 Before you save — a few things are missing (optional)" (`data-testid="presubmit-title"`), copy "Nothing here is mandatory. You can save now and add the photos or actions later from History → ✏️ Edit", buttons `presubmit-back` "Go back & add them" and green `presubmit-save` "💾 Save anyway" (= `onTransform(true)`); `Go fix →` per row kept. No `hard` path remains. Wording: fail-panel hints "Recommended — say what was done about it" / "Recommended — add a BEFORE photo of the problem", `.caSub` "Answer what you can — anything left stays open in Follow-ups.", sink/food-temp corrective hints "Recommended — …", "(required)" dropped from the three description placeholders, stars removed from Supervisor and License (Inspector / Date / Restaurant Name keep theirs — `validateForm` warnings only scroll). "— required internal temp before serving" (cook temps) is a food-safety fact and stays. Harness `train/v497.mjs` (v477 head; RUN THE OPEN-PROBLEM FLOW FIRST — a local-mode save wipes the seeded history that feeds `standOpenProblems`): fryer NO with nothing → modal → Go back keeps the form → Save anyway saves (draft gone); SOL CUBANO #350 open problem unanswered → listed → saved; complete form → no modal. v493's "blocked" assertion now expects the soft modal; v459 passes. (b) Samantha Fragoso (badge hash `d63536…8bf02`) had reopened her June 3 PANTRY #307 report `1780508024322_b7ixzp` in Edit mode on Sep 24 (~19:26Z) and could not save (10 NO rows without row photos → hard modal). Her cloud draft (`sharedMemory/inspectionDrafts` key `<hash>__1780508024322_b7ixzp`, 20:21Z; photo arrays stripped by `slimDraft`; Storage listing is 403 from here) was rebuilt in scratchpad `sam2/`: `rec_existing.json` (REST GET, June updateTime), grafting the record's 18 photo refs back by path (`sam_draft_grafted.json`), `build.mjs` (v497 dist, local mode, seeds `sdx_drafts_default` + `sdx_report_id_default` → v495 auto-restore → `__sdxFailNextSave` → Save → Save anyway → the clean record from `sdx_inspection_outbox_default` = `record_out.json`: 12 action items, photoCount 18, license blanked for Pantry, overallStatus Pass as the app computes), `send.py check|send` (savedByHash → hers, savedAt now, REST PATCH with `currentDocument.updateTime` precondition, read-back OK 20:37Z). inspectionDate stays 2026-06-03 (it is her June report). Her phone still holds the draft with today's photos: once she has v497 (close/reopen twice) the v495 migration restores it and Save anyway overwrites this record with the photo version. The cloud draft was left untouched.
- v496: the guide section chips are colour-coded and read as progress, not problems. Joxel (screenshot of "1 Temps · 2 Facilities 81 · … 4 Equipment 70 · 5 Utensils 26" with red counts): "make this better … putting some color sometimes is hard to find things and I also want them to not feel like is going to take all day". The red `81` was the number of UNANSWERED rows styled like an error count. Now (stepper header ~33253): `STEP_META` per panel = icon + colour (Temps 🌡 blue, Facilities 🏢 teal, Maintenance 🔧 orange, Equipment ❄ indigo, Utensils 🍴 violet, Operations 📋 green) set as `--chipC/--chipBg` on `.guideStepChip` with `data-state` todo / started / done / active (`data-testid="guide-chip-<pid>"`): todo = white/grey with `0/81`, started = the section's tint with `12/81`, done = green `✓ done`, active = filled with the section colour; a red `⚠ N` badge appears ONLY for rows marked NO (`panelCounts[pid].issues`) or bad maintenance statuses. `.guideEase` line under the title: "Answer only what you see — anything you skip is simply not checked. Most walks take about 10 minutes." Progress bar text `.guideProgressText` = `N checked · ⚠ N issues · N left` (temps typed count as checked via `tempsDone`). Harness `train/v496.mjs`: six chips with icons, no red badge on a fresh form, `0/N` labels, reassurance line, a NO + a YES on fryer rows → Equipment `⚠ 1 2/70` started, progress text, Facilities active = teal. Screenshots `v496/`.
- v495: the report saves itself on EVERY change, and nothing in progress can be lost. Joxel: "auto save every time they do something in a report … there has been a lot of missing reports". ROOT CAUSES: the v451 draft was written 4 s after the last change and only when notes / a stand name / a photo existed (checklist-only and temps-only walks never drafted), it skipped `suppliesNeeded`, `notesPhotos`, `foodTempCorrections/Submitted/Times`, `correctives`, `output`, `onSiteConfirmed`, the timers, was never flushed on `pagehide` / lock, sat in ONE slot (`sdx_draft_<venue>`) that ➕ New Inspection, ✏️ Edit and a venue switch cleared without asking, was only OFFERED back (banner), and a failed Save only showed an error. Now (module level next to `slimLocalDraft`): `DRAFTS_KEY = sdx_drafts_<venue>` = `{[reportId]: draft}` (`loadDrafts/saveDraftFor/removeDraftFor/listDrafts/draftTitle`, quota → retry without pictures (`photosDropped`) → `false`), `migrateLegacyDraft()` moves the old key in; cloud drafts keyed `<badge>__<reportId>` (`draftCloudKey`, `loadDraftsCloud(badge)`, `clearDraftCloud` uses `deleteField`). Form shell: `buildDraftSnapshot()` (every field above + `guideStep`, `reportStartedAt/inspectionStartedAt`, `isEditMode`), `pristineRef` / `armPristine()` (set at New / restore / save — the next render records the pristine JSON), the draft effect writes SYNCHRONOUSLY whenever the slim JSON differs from pristine and `hasContent` (stand / unit / notes / supplies / photos / temps / an inspection that differs from `buildDefaultInspection()` — signing in alone never drafts); cloud copy throttled 8 s (`scheduleCloud/sendCloudNow`); `flushDraftRef.current()` (= `window.__sdxFlushDraft`, also called by the SW "Reload now" in main.jsx) on `pagehide`, `visibilitychange hidden`, `beforeunload` (which now warns for anything dirty), idle lock, Lock App, page change, `startNewInspection`, `loadRecordForEdit`. After unlock the draft for `REPORT_ID_KEY` is restored by itself (`openDraft(d, {auto})`, `.draftRestoredStrip` "↩ Restored your unsaved report from …"); other drafts (local + cloud ☁) show in `.draftListBar` "📋 N unsaved reports" (`data-testid="draft-list-toggle"`, rows `draft-row` with Open / 🗑 Delete → inline `.draftAsk` confirm; deleting the open one starts a new form). `restoreFormState` also restores `notesPhotos`, `correctives`, `onSiteConfirmed`, `guideStep`. Strip (`data-testid="draft-strip"`): `✓ Saved on this phone · just now · ☁ cloud HH:MM` (`draft-ok`), red `⚠ Could not save on this phone — free up space…` (`draft-fail`), `✅ Report saved`. Failed Save (not permission-denied / too-large): `queueInspection` → `sdx_inspection_outbox_<venue>`, the record goes into `sdx_history_cache` with `_pending` (History card `.pendingUploadChip` ⏳ Waiting to upload), toast "📥 Saved on this phone — it uploads when the connection is back", `.outboxChip` "📥 N reports waiting to upload · tap to retry"; `flushInspectionOutbox()` (= `window.__sdxFlushOutbox`) runs on unlock, `online`, every 30 s and after each save, then `learnFromSave` + `notifyCrewsForItems` via `_inspOutboxOnSent`, fires `sdx-outbox {count, sent}` (toast "✅ N reports uploaded"). `afterSaved(id, queued)` = the post-save form reset; `stripBase64` now blanks only `data:` strings and strings > 200 KB (was 8 KB — long notes were blanked). `window.__sdxFailNextSave = true` makes the next `saveOneInspection` throw (harness). NOT covered: photos whose Storage upload failed are still dropped at save time. Harness `train/v495.mjs` (v477 head; `pick` / `fillAction` helpers live after line 49 — copy them; fill inputs through the native setter after New Inspection because the smooth scroll makes Playwright clicks time out; the on-site overlay covers the list after New): draft at once with NO row / photo thumb / action / supply / note / onSiteConfirmed, pagehide, quota red→green, reload → auto-restore (same id, supplies kept), New → 1 unsaved, Open → back, SEED flushed to its own slot, delete confirm; failed save → toast / outbox / pending card / chip → flush; normal save removes the draft. v451 / v477 read `sdx_drafts_default` now; v493 / v459 / v460 pass.
- v494: painting is Building. Joxel: "And painting peeling paint and all of that is building". `BUILDING_RE` gained `paint(ing|ed)`, `repaint*`, `pintura`, `pintar`, `pintad*`, `peeling`, `chipp(ed|ing)`, `descascar*`, `despintad*` (the damage-word requirement stays for tiles / walls, so "dirty walls with grease" is still Cleaning). Harness `train/v494.mjs`: 8 sentences EN/ES → Building, greasy walls → Cleaning; v490 passes.
- v493: short corrective actions no longer block, draft beer for bars and pantries, WIC in the pantry list. Joxel (pre-submit modal "…No missing tiles / panels: needs a corrective action" while the row had a BEFORE photo and "Bejs" typed): "Dealt beer for bars and pantries please put WIC in pantry's predetermine and also there is a problem submitting problems". ROOT CAUSE of the block: `proofGate` required `action.trim().length >= 5` (v439) — a 4-letter action was rejected while looking filled in, and the fail panel's red border / "⚠ Required — say what was done about it" hint used the same `< 5`. Now any non-empty action counts (photo still required); the border/hint show only while the box is empty. `PANTRY_COLD_EQUIPMENT` gained `walkInCooler` (cooler 40) + `walkInFreezer` (freezer 20); the Pantry `GuideSection` gained "Walk-In Cooler — …" and "Draft Beer — taps / lines cleaned, kegs dated, drip tray clean, no leaks?" (`equipment.draftBeer`); the Bar `beerLines` row (same key) reads "Draft Beer / Beer Lines & Taps — …"; `ITEM_LABEL` + the escalation equipment list know `draftBeer`. Harness `train/v493.mjs` (v477 head): fryer row NO + BEFORE photo → Save with an empty action = blocked, "Bejs" → no hint, Save passes the gate; Pantry panel lists Walk-In Cooler (❄) + Draft Beer, Bar reads Draft Beer / Beer Lines, Concession unchanged. NOTE: the first successful Save in a harness saves the report — test the blocked case BEFORE the passing one. v475 / v476 / v477 / v489 pass.
- v492 / Sep 23 cleaning day closed (2026-09-24): Joxel pasted the WhatsApp log with Carlos (cleaning crew) — 355 14:01, 336 14:07, 308 13:36–14:36 (1 h), 214 = 2C 14:52, 242 16:21, then 145 and 134 (all seven confirmed done). App change: `window.__sdxComputeFollowups = (h, vs) => computeFollowups(h, vs)` (next to `__sdxBuildActionItems`) — scripts can derive follow-up keys from live data. Scratchpad `clean0923/`: `fetch.py` (REST: inspections since Sep 1 → `history.json`, `venues/default/sharedMemory/venueSettings` → `venueSettings.json`), `compute.mjs` (v485 head seeded with the LIVE history + settings → `followups.json`), `resolve.py check|send` (25 Cleaning-type keys at 134 / 308 (CANTALOUPE keys cover 336 too — follow-ups key by NAME::cat) / 355 / 242 / 145, skipping already-resolved keys and the FUKU walk-in temperature; PATCH with `updateMask.fieldPaths` = ``followupStatus.`KEY` `` etc. (backtick-quoted, URL-encoded); entries `{status:"resolved", by:"CARLOS", ts:<EDT log time>, how:"crew", whose, standType, startTs/mins for 308}`, `followupCleared = max(ts, f.ts+1)`, a 🧹 comment; read-back 25/25). `export.mjs` = Follow-ups → Select to export → ✅ Fixed panel → tick rows containing CARLOS → 📊 Excel + 📸 Before & After captured via the `URL.createObjectURL` stub; `finish.mjs` adds "Time log Sep 23" + "Supplies used" sheets (SEED #117 report supplies = Orange Force Oasis, Wash 'n Walk, test strips; 114A / 111 needs) → `Cleaning-crew-2026-09-23.xlsx`; the B&A workbook from here embeds only 1 photo (Storage fetch is limited in the sandbox — export it from the app for pictures). Email text `email-cleaning-0923.md`. 2C / #214 has no report on file → nothing to close.
- Notes → reports, done 2026-09-24 (no app change): Joxel pasted his Apple Notes text for 10 stands (Sep 22 walk, date confirmed by him; photos to come later). Scratchpad `notes/notes.txt` (one line per stand; the 347A "Agua frente al freezer #24 Temp 28.6" segment was split by hand into an issue + a reading), `notes/build.mjs` (v485 head → `__sdxParseNotes` + `__sdxBuildImportRecord(note, {date:"2026-09-22", inspectorName:"Joxel Da Silva"})` → `records.json`), `notes/send.py check|send` (GET each deterministic id + runQuery on `inspectionDate == 2026-09-22`, then POST `inspections?documentId=` with a read-back). NOVECENTO #134 ("Limpieza de la campana") was SKIPPED — his full Sep 22 report `1790106122946_dz73jb` already carries three Hood items. Created (ids `notes_20260922_<unit>_nN`): CANTALOUPE 308 (10 items, coolers 44.8 / 53.5), AIFI SMASHED BURGER 345A, AIFI EDGEWATER GRILL 347A (cooler 48.9), LUCKY CHICKEN 347 (freezer 50.9), LITTLE CAESAR 345, CANTALOUPE 336 (coolers 44.5 / 45.2 / 48.0), SEED 332, FAN FAVORITE 355 (freezer 65.9), SOL CUBANO 350. Freezer readings 26–28 °F are flagged by the app's 20 °F freezer limit (v480 note). When the photos arrive: Storage `photos/default/<recordId>/<photoId>.jpg` + PATCH the record's `actionItems[].photos` / `photos` (same path as v478).
- v491: stand names never get translated. Joxel (History screenshot "TWENTIETH CENTURY #134 NOS2325137"): "the name of novecento is changing" — the page translator (v452/v487 family) translated the Italian stand name. New `NT` component (next to `StandType`) = `<span className="notranslate" translate="no">`; wrapped: History card title (name / #unit / 🪪 license), `.rptSiteName`, `.crewReportSite`, Follow-ups group head + card stand line + `.fuFixedTitle`, Timeline event label + sub, `.predictiveItemMsg` (whole sentence — it embeds the stand), HACCP tracker `.haccpStandHead` name, equipment page `.walkStandHead` spans (3), posters card `🍳 {k.site}`, `.kqrPeopleStandHead`, the TextStepper head, the equipment-history site line; print/HTML pages get `<html lang="en" translate="no">` and the report `<strong>` name. Inputs never translate (form fields untouched). Harness `train/v491.mjs` (v485 head, seeds NOVECENTO #134 NOS2325137): a fake translator replaces every text node outside `.notranslate` with a `<font>` copy (NOVECENTO → TWENTIETH CENTURY) on History list, expanded card, Follow-ups, Timeline, Posters → name intact, plus a structural sweep for unprotected `NOVECENTO` leaves (SKIP when the page has no such stand — Predictive / Temps / Equipment in that seed). v487 / v473 / v471 / v464 pass. Remaining unwrapped sites print names inside sentences (reminders, flashes) — add `NT` when reported.
- v490: "Building" is an issue type. Joxel: "I want to add building option for things that are like a whole on the wall and all of that". `ISSUE_TYPES` gained `Building` (🏗, after Maintenance; `EXPLICIT_TYPE.building`, `issueTypeStyle` lavender) and `BUILDING_RE` (next to `ISSUE_TYPES`: holes / hueco / agujero, drywall, missing·broken·cracked·loose·falling ceiling/floor/wall tiles (EN + loseta/losa/azulejo), baseboard / cove base / grout damage, peeling paint, structural, door frame, water / wall damage, pared rota, techo caído, broken window — damage words are REQUIRED so "ceiling tiles dirty" and "window dirty" stay Cleaning). `classifyIssueType` returns Building for `priority "Building"` or the regex, checked after Ecolab and before Maintenance; `NLU_CATS` has a Building entry before Equipment and `NLU_OPT_SYN` knows the new chips. FOUND: two lookups used `NLU_CATS[4]` / `NLU_CATS[6]` by INDEX (Equipment / Cleaning) — inserting a category silently broke "grease under the fryer" → they are `NLU_CATS.find(c => c.cat === …)` now; never index that table. Checklist rows: the per-row 📋 STATUS select and `STATUS_OPTIONS` offer "Building" → `priority "Building"` in `buildActionItems` (the three `sectionStatus === "Maintenance"` ternaries and the isFail / bad checks include it). Crews: `CREW_TYPES.maintenance = ["Maintenance", "Building"]` (no new role — the maintenance board shows Building cards with 🏗; "Not mine → move" and the follow-up type menu list it). Export "who is it for": `exportCrewOf` → "Building", `CREW_BTNS` gained 🏗 Building (`data-testid="exp-crew-building"`, filename `inspection-building-issues-…`). Follow-ups summary: `🏗 N building` chip (`.fuCrewBuilding`, filters by search "building"). Quick report / portal: `QUICK_PROBLEM_CATS` + `SUP_PROBLEM_CATS` (🏗 Building / Edificio · Estructura), `specCatKey` → "Building", `SPEC_ROWS.Building` (what: Wall / Ceiling / Floor / Door / Door frame / Window / Baseboard / Paint; problem: Hole / Crack / Missing tile / Loose tile / Peeling paint / Water damage / Broken / Falling; where incl. Front / Back of the house, Mop area). Harness `train/v490.mjs` (v485 head): 8-sentence table (holes / hueco / missing tile / paint / loseta rota → Building; dirty tiles, dirty window → Cleaning; handle → Maintenance), `__sdxBuildActionItems` with `ciStatus "Building"` → priority Building, Follow-ups chip + card + type menu, export step Building 1 / Cleaning 1 / Maintenance 1, maintenance board card with 🏗. v484 expects 5 crew buttons now; v467 taps `.crewBigBtn` too; v485 / v456 pass.
- v489: the Ecolab products ARE the inventory, and "Front / Back of the house" are places. Joxel: "add the ECOLAB products to the list … detergent, sanitizer, cleaning, and sanitizer Orange Forest . Paper towel . Hand soap" → "I said that because I want to do inventory faster and also … add 'back of the house' and 'front of the house'". (a) New Facilities node `facility.ecolabProducts` (`CHECKLIST_DEFAULTS.ecolabProducts`, after `mopArea`; registered in `CHECKLIST_KEY_MAP`, `buildDefaultInspection`, the path table "Facilities > Ecolab Products", the label map, the three section tables, `checklistSections`, `checklistSectionsIssuesWord`): Detergent (pot & pan) · Sanitizer · All-purpose cleaner / degreaser · Orange Force ("Forest" was a typo — the Ecolab product is Orange Force) · Paper towels · Hand soap · Test strips · labeled & stored below food contact · Other. Guide item "Ecolab Products & Supplies — …" after Mop Area in the main form AND in the Bar, Pantry and Portable lists (`PORTABLE_EQUIP_ITEMS`), so every stand type has it. Rows carry `supply: "<product>"`; `makeSet` in `GuideSection` fires `sdx-supply-needed {item, on}` when such a row flips, and the form shell (next to `suppliesNeeded` state) adds `{item, qty:"", urgent:true, fromChecklist:true}` on NO (no duplicates) and on YES removes ONLY rows it added (`fromChecklist`) — a typed supply stays; those rows show `.supAutoChip` "🧪 from the Ecolab checklist" (`data-testid="sup-auto"`). `classifyIssueType` + `NLU_CATS` Ecolab gained orange force / degreaser / paper towel(s) / hand soap / test strips (EN+ES). `window.__sdxBuildActionItems` is exposed for harnesses. (b) Locations: the checklist fail panel 📍 SPECIFIC LOCATION chips start with "Front of the house", "Back of the house"; every `SPEC_ROWS` `where` row says "Back of the house" (was "Back of house") and gained "Front of the house / Frente del puesto"; `NLU_AREAS` gained "Front of the house" (foh / frente del puesto / parte de adelante) and "Back of house" (canonical label kept for stored data) accepts "back of the house"; verify-walk `WALK_LOCS` too. Harness `train/v489.mjs` (v477 head, Facilities panel `[data-guide-panel="1"]` forced visible + `sdx-open-guide-item` `facility.ecolabProducts`): 9 rows, NO on Detergent + Orange Force → both in Supplies Needed with 2 auto chips, YES on Detergent removes only it (typed "Gloves" stays), chips Front/Back first, `__sdxBuildActionItems` → `Facilities – Ecolab Products: Orange Force is missing or empty [Back of the house]`, NLU areas/routing, Bar / Pantry / Portable / Kitchen show the node; v475 / v476 / v459 / v477 pass. Screenshot `v489/ecolab-rows.png`.
- v488: crews get in from the link — no badge — and the board is simple. Joxel: "let the cleaning crew and the maintenance crew to log in when I sent the link without any log in and also make it more user friendly I have some user that do not understand". (a) The role invite link (Admin → 🔗 Invite links, `venueSettings.inviteTokens[role]`) opened for maintenance / cleaning / ecolab (`CREW_LINK_ROLES`) now shows `CrewJoinCard` (next to `lockApp`; EN/ES/HT pills via `CREW_JOIN_STR`): ONE name box + "Open my board". `signInCrewLink(session)` writes/refreshes a `users` doc keyed `badgeHash = "link_<uid>"` (`viaLink`, `badgeDisplay "🔗 link"`, approved) and signs in; the device keeps `sdx_crew_session_<venue>` = `{uid, name, role, token}`. The App shell effect (next to `resetActivity`, deps `[locked, venueSettings.inviteTokens]`) re-signs a saved session on EVERY load when `inviteTokens[role] === token` (`crewNotice "opening"` hides the badge form meanwhile; `window.__sdxCrewAutoOpened` counts); a mismatch (↻ New on the invite card) clears the session and shows `data-testid="crew-replaced"` "This link was replaced — ask the inspector for the new link" (unless the NEW link is what they opened — then the join card, same uid). The idle timer never locks a link session; ☰ "Lock App" reads "Sign out of this phone" (confirm) for link users (`linkSession` prop) and clears the session. Invite card copy says crews need no badge; crew rows gain `📱 Text` (`sms:?&body=` EN/ES + link). Inspector / manager / guest invites keep the badge flow. Security unchanged (public bearer token) — but regenerating now really signs everyone out. (b) `CrewBoardPage` simple view (`sdx_crew_simple`, default ON, `.crewSimple`): `.crewHello` "👋 Hi NAME · N things to do · ⏰ overdue" + how-to line + `⚙ More options` (`data-testid="crew-more-options"`, also in ☰), hides tabs / stats / search / stand select / By Problem / floor chips / show fixed / other (5 tappable controls above the first card at 390 px, was ~14; entering simple resets those filters); Reports reachable from ☰ with a "← Back to my list" button; card = two 52 px buttons `🔧 I'm working on it` (`data-testid="crew-work"`, `.on` when in process) and `✅ Done — add photo` (`crew-done`), `more…` reveals ⏳ Waiting and Not mine → move; the done box is numbered (1 📷 photo · 2 cleaned/already clean for cleaning · 3 note optional · 4 ✅ Send, `data-testid="crew-send"`). The NOTE is now optional for ✅ Fixed in every view (the after photo is the proof; still required for ⏳ Waiting). Harness `train/v488.mjs` (v451 head + `inviteTokens` seed): join card without `.badgeInput`, link user + session written, URL cleaned, reload → board with no sign-in, 11 min fake idle → still the board, big buttons, done flow with empty note → resolved entry by ANA LOPEZ, More options ↔ Simple view, Spanish hello, token change → replaced card + session null, new link → in again, sign out → pin screen; inspector invite still shows the badge form; badge crew user (777001) also gets the simple view with "Lock App". v451 / v456 / v442 harness selectors widened to `.crewBtn, .crewBigBtn` + `Done|Listo` (v451, v456, v473 pass; v442's crew step was already stale since v454 — it types a no-photo excuse — v451 covers it).
- v487: "sometimes when i use too much the filter it logs me out" (screenshot: the ErrorBoundary card translated — "Algo salió mal · El objeto no se encuentra aquí · Recargar la aplicación"). ROOT CAUSE: "The object can not be found here" is Safari's NotFoundError from React's `removeChild` / `insertBefore` after Google Translate swapped a text node for its own `<font>` (same family as v452); every filter change re-renders the History chips/list until a removal hits a translated node → boundary → looks like a logout (the session is intact). Fix in `src/main.jsx` before the mount: `guardDomAgainstTranslator()` wraps `Node.prototype.removeChild` / `insertBefore` to no-op when the child/ref is no longer ours (facebook/react#11538 workaround); the boundary card now says "You are still signed in — reloading brings you back where you were." Harness `train/v487.mjs`: every text node outside `.notranslate` replaced by a `<font>` copy, six rounds of select/search filtering on History → no crash screen, 0 page errors, cards back after clearing. NOTE: the Vite build strips `console.*`, so the guard's fingerprint in `dist` is `parentNode!==this`, not the log string. Also delivered (no code): the Spanish version of the cleaning follow-ups Excel (`scratchpad/train/es_followups.mjs` → xlsx + csv, 35 rows).
- v486: the bulk Excel summary shows the scores. Joxel (screenshot of the "Inspection Summary" sheet): "i want to show the scores here". `exportBulkExcel` sheet 1 gained `Score` (the `calcInspectionScore` pct as a real number with `0%` format), `Grade` (A–F) and `Passed / Scored` (e.g. `8 / 13`) after Overall Status (cols 13–15; Total Issues / High Priority moved to 16–17; title merge A:Q, autoFilter to 17, widths added); cells tinted by grade via `gradeStyle` (A green, B yellow, C orange, D/F red); records without a checklist (quick reports, supervisor logs) show `—`; an `AVERAGE SCORE — N scored reports (best X%, lowest Y%)` row (B:L merged) closes the sheet with the average and its grade. Harness `train/v486.mjs` (= v484 + reads sheet 1 back): headers, Samantha's report 71 % / C / `12 / 17` (grade and fraction checked against the percentage), a quick report `—`, the average row.
- v485: rust is a cleaning job. Joxel: "lets put the rusted things in cleaning". `classifyIssueType` (3849): `rust\w*|oxidad\w*|corro(sion|ded|ido)|herrumbre` moved from the soft-maintenance regex into the Cleaning regex (Cleaning is tested before soft maintenance, so "rusted shelf" / "estante oxidado" / "corroded rack" → Cleaning; "broken handle" stays Maintenance); NLU_CATS: the same words left Maintenance and joined Cleaning, so `understandText` / the portal chips / the notes pass agree. Everything downstream (crew boards via CREW_TYPES, follow-up `itype` + crew chips, the export crew step, Excel "Issue Type") derives from these two rules, so existing open rust items move to the cleaning crew automatically (no data change). The checklist row "No visible damage or rust" now classifies as Cleaning too. Harness `train/v485.mjs`: `__sdxUnderstand` on rusted / oxidado / corroded → Cleaning, broken handle → Maintenance; a seeded quick report's rust card shows the Cleaning crew and the handle card Maintenance; the export crew step counts 1 Cleaning / 1 Maintenance.
- v484: the bulk Excel export asks who it is for and is fast. Joxel (History → Select → Export to Excel, "Preparing download…" for minutes): "we need to download the information faster and it should ask me what i want first: cleaning, maintenance, ecolab". ROOT CAUSES of the slowness in `exportBulkExcel`: it fetched EVERY photo of EVERY selected report (`buildPhotoIndex` → `fetchAsDataUrl` = the full 3–4 MB originals) and then fetched the same photos AGAIN per action item (no cache), regardless of which issues were ticked, embedded them full-size (a 100 MB+ workbook), and only after the HACCP reads (`loadHaccpForReport` + `loadHaccpBySite` per record) had finished. Now: `makeExportPhotoFetcher()` (next to `fetchAsDataUrl`; `fetchPhotoBlob` = SDK getBlob / fetch / data:, `shrinkBlobToDataUrl` = createImageBitmap → ≤480 px JPEG q0.72 ≈ 40 KB; per-export cache by URL; ≤6 in flight; `window.__sdxExportPhotoStats` = requested/fetched/cached/failed) — the export takes ONLY the ticked issues' photos (`doExport` already narrows `rec.actionItems`; the photo index is built lazily only for numeric refs), at most `EXPORT_PHOTOS_PER_ISSUE` = 3 each, while the HACCP reads run in parallel (`Promise.all([haccpPromise, photoPromise])`); `exportBulkExcel(records, { includePhotos, crew, onProgress })`; pictures off → the Photos column reads "N photos in the app"; filename `inspection-<crew>-issues-<date>.xlsx`; `console.info("[export] photos … in N ms")`. Modal (HistoryPage, state `exportCrew` / `exportPhotos` / `exportProgress`, reset when the bulk bar opens it): step 1 `.expCrewStep` "Who is this export for?" — 🧹 Cleaning crew / 🔧 Maintenance / 🧪 Ecolab / 📋 Everything with counts (`exportCrewOf(a)` = `classifyIssueType` → Cleaning; Maintenance + Temperature → Maintenance; Ecolab / Maintenance → Ecolab; Pest / Other only in Everything, noted under the grid); `pickCrew` ticks exactly that crew's keys; step 2 = the old list with `.expCrewBar` (crew chip, Change, `📷 Pictures in the file (N)` toggle with "off = instant" when N > 30) and the button shows `Pictures 12 of 40…` / `Building the file…`. Bulk PDF / Word never embedded photos — untouched. Harness `train/v484.mjs` (v478 head, inspector 448800, 1100 px; remove `.swUpdateBanner` and `click({force:true})` on the bulk bar — the banner covers it): 4 crew buttons with counts, Cleaning → exactly its count ticked, pictures off → 0 images, `cleaning` in the name, Photos column text; Everything + pictures → images embedded, each < 60 KB, `stats.fetched === images` and `requested ≤ ticked × 3`.
- v483: the Import notes FEATURE is gone. Joxel: "i do not want to add that as a feature in the system i just want to get that done". Removed `ImportNotesPage`, the `import_notes` page key, the History header / ⋯ menu / ☰ entries, `public/tools/` (the served script) and the `.imp*` CSS. KEPT as an internal, UI-less tool: `parseInspectionNotes` / `buildImportedRecord` / `resolveImportStand` / `importGroupFolderFiles` / `importFilesFromTransfer` (exposed as `window.__sdxParseNotes`, `__sdxBuildImportRecord`, `__sdxImportGroupFolder`, `__sdxImportFilesFromTransfer`) and `docs/tools/export-apple-notes.applescript` (how Joxel gets text + pictures out of Apple Notes). The way to "get it done": Joxel sends the notes (the exported folder, or text + photos in chat) → a harness runs `__sdxBuildImportRecord` per line in the built app (local mode) to produce records → photos go to Storage `photos/default/<recordId>/<photoId>.jpg` (rules: no auth, image/*, ≤10 MB) and records are POSTed to `inspections` via REST (create allowed by `validInspection()`), one per stand, with a read-back check — same path as the Samantha correction. The NLU words added in v480 (mojad*, agua, charco, delante, mostrador(es), interior, estante) stay. Harnesses v480–v482 no longer apply (they drive the removed page); v479 / v467 / v478 are the regressions.
- v482: everything out of Apple Notes in one drop. Joxel: "i want to collect all of the information from my notes how can i do that". `docs/tools/export-apple-notes.applescript` (also served at `/tools/export-apple-notes.applescript` from `public/tools/`): run in Script Editor on the Mac → `Desktop/SDX Notes Export/NN - <title>/note.txt` (`M/D/YY h:mm AM/PM ` + the note's plaintext) + `photo-NN-<name>.jpeg` (attachments via Notes' `save … in file`; file/shell calls live in `my writeText` / `my makeDir` handlers so they run outside the `tell application "Notes"` block; `safeName` keeps dots so extensions survive) + `all-notes.txt`. Importer: `IMPORT_HEAD_RE` now takes an optional `M/D/YY` date before the time (groups: 1 date, 2 time, 3 unit) → `note.date` (`importDateIso`) wins over the page date in `buildImportedRecord`; review shows a 📅 chip when it differs. Paste step gained `📁 Drop the “SDX Notes Export” folder here` (`data-testid="imp-folder-drop"`, `<input webkitdirectory>` fallback) → `importReadEntry` walks `webkitGetAsEntry()` directories → `importGroupFolderFiles(list)` (`window.__sdxImportGroupFolder`; one group per sub-folder: `.txt` lines joined with ", " + its pictures; `all-notes.txt` ignored; a flat folder with one text file = one note) → `loadFolder` (`window.__sdxImportLoadFolder`) parses each group's line separately (keeps index alignment), jumps to review and attaches the pictures through `addPhotos` sequentially. `<details class="impHow">` explains the 4 steps with `📋 Copy the export script` (fetches the file, `navigator.clipboard`). Harness `train/v482.mjs` (= v481 + dated line → `notes_20260922_345A_1740` with inspectionDate from the line; grouping unit test; a synthetic export folder through `__sdxImportLoadFolder` → 2 stands with 1 and 2 thumbs, 17:15 kept, a two-line note = two segments). Real OS folder drags and the AppleScript cannot run here.
- v481: the importer takes pictures any way they can come out of Apple Notes. Joxel: "get the images too" (I cannot reach iCloud; the photos live only in his notes). `importFilesFromTransfer(dt)` (next to the parser, `window.__sdxImportFilesFromTransfer`) tries `dt.files` (Finder / Notes-app drag / the picker), then `dt.items` kind "file" (`getAsFile()` — Safari page-image drags, ⌘V of a copied picture), then a `text/uri-list` / `<img src>` from `text/html` fetched with CORS into a File (an `<img>` dragged from another tab; iCloud image URLs will fail cross-origin → the box says "save it first, then tap to choose"). The review page keeps `activeIdx` (the stand tapped last, `.impActive` blue ring, hint "or copy a picture and press ⌘V") and a document `paste` listener (skips INPUT/TEXTAREA targets) that attaches clipboard pictures to that stand. Harness `train/v481.mjs` (= v480 + a synthetic `ClipboardEvent('paste', {clipboardData})` with a JPEG File → thumb on stand 347, a `text/uri-list` fetch of `/Claude/icons/icon-192.png` → one image File). Chromium's `new ClipboardEvent(..., {clipboardData})` and `new DataTransfer()` work headlessly; real Safari behaviour for Notes-app drags is unverified here.
- v480: 📥 Import notes → reports. Joxel (iCloud Notes screenshot, one note per stand: "Stand 345A License NOS2334857 Refrigerator Dos Puertas #4 Temp 33.0^F, Freezer Dos Puertas # X Temp 13.4^F, Freezer Dos Puertas #2 Temp 12.7^F, Agua Frente al Freezer #2" + photos + a time): "i want to add all of this information as reports" / "i just want to create a report out of those notes". New page `import_notes` (`ImportNotesPage`, after `processPhotoFiles`; entry = History header `📥 Import notes` (`data-testid="hist-import"`, non-crew), the History ⋯ "Go to" list and the ☰ Work section): paste all notes → `parseInspectionNotes(text)` (`window.__sdxParseNotes`) → review cards → `Create N reports` → `saveOneInspection` per record SEQUENTIALLY (never `saveHistory(list)` — that rewrites everything online and wipes the local vault), then `learnFromSave`, `notifyCrewsForItems`, prepend to `sdx_history_cache_<venue>`, `_speedCache.history = {}`. Parser: `IMPORT_HEAD_RE` (`Stand|puesto|unit 345A`, optional `5:40 PM` prefix → `time` 24 h; the unit letter is taken only when not the start of the next word, so "Stand 345 License" stays 345), `IMPORT_LIC_RE`, `^F`/`º` → `°F`, segments split on `, ; | / -` then before each cold-unit word when a piece holds 2+ readings; a reading = `Temp 33.0°F` or `33°F` (°C converted); the rest of the segment is the unit label UPPER-CASED; `importColdType` (freezer: freezer/congelador; cooler: refri*/nevera/cooler/fridge/reach-in/walk-in); zones use the app's equipment limits cooler ≤40 / freezer ≤20 (warn 45 / 25 = `TEMP_WARN_MAX`), NOT the 41 / 0 printed on posters; unknown type ("#24 Temp 28.6") → `type?` select in the review + warning. Non-temp segments → `understandText` → issues (category / crew / area). Stand: `resolveImportStand(unit, license)` = license number first, then a single-row unit; `unitMismatch` (license belongs to another unit, e.g. NOS2331746 is 219 not 219A) and `licenseMismatch` warnings; not on the sheet (341, 219A) → red row with a name input, blocked until typed. Record (`buildImportedRecord`, `window.__sdxBuildImportRecord`): id `notes_<yyyymmdd>_<unit>_<hhmm|nN>` (deterministic → a re-import overwrites), `savedAt` = date + note time, `inspectionType "Regular Inspection"`, `source "notes_import"`, `noteTime`; `inspection` = `buildDefaultInspection()` with EVERY default node `status ""` (not checked — the notes only cover what they say; with no scored node `calcOverallStatus` falls back to the status walk), temps as `equipment.custom_<ts>_<i>` `{label, tempF, status "OK"}` (label prefixed COOLER/FREEZER when `detectColdType` would not recognise it), issues as `facility.custom_<ts>_i<i>` `{label: category, status "Needs Attention", notes: text, kitchenArea: area}` so `buildActionItems` derives `Facilities – Cleaning: Agua Frente al Freezer #2` and its v467 notes pass dedupes on the sentence; `rawNotes` = `Stand #345A · imported from notes: <line>` (the `Stand #` prefix makes `understandNotes` skip it); photos via `processPhotoFiles` into `_notesPhotos` (http URLs only; local mode keeps none) and onto every stored action item; `overallStatus` = issues or a bad reading → Needs Attention. NLU vocabulary added for the notes: Cleaning `mojad*`, `agua`, `charco*`, `encharcad*`; Front line `delante`, `mostrador(es)`; Inside `interior`; Standing water `agua|mojad*`; `classifyIssueType` Cleaning `mojad|agua|charco|estante|mostrador`. Harness `train/v480.mjs` (admin 365582, 1100 px): 10 reconstructed lines through `__sdxParseNotes` (stand / type / floor / by-license, 3 readings + 1 Cleaning issue on 345A, 17:40 on 347A, #24 unknown → pick freezer → `✗ 28.6°F · max 20°F`, 341 blocked → typed name, 219A mismatch flagged, "26," dropped), a photo per `setInputFiles`, Create → 10/10 saved (local vault grows), History cards incl. the typed stand, the 345A card shows 33°F + the water issue, Follow-ups groups (expand `.fuGroupHead` first — collapsed groups hide the issue text) show the water issue and `Temperature out of range … #24`. Photos in cloud mode upload to `photos/<venue>/<recordId>/` (storage rules: no auth, ≤10 MB, write-once) — not testable here.
- v479: tapping a checklist photo thumbnail in the inspection form now opens the full-size viewer. Joxel (form screenshot, Mac Safari): "from here i cannot click to expand the pic". ROOT CAUSE: the `.ciPhotoStrip` thumb `onClick` inside `GuideSection` called `setAppLightboxSrc`, which is defined only in the App component — every tap threw `ReferenceError: setAppLightboxSrc is not defined` (caught by the harness as a PAGEERR) and nothing opened; the report preview's own `.rptPhotoImg` lightbox was fine, which is why it looked Safari-specific. Fix: module-level `openPhotoLightbox(src)` (next to `cx`) dispatches `sdx-lightbox`; the App shell's `useEffect` (next to `setAppLightboxSrc`) sets `modals.lightboxSrc`; the strip calls it with `previewUrl || exportUrl || url || dataUrl || thumbUrl`. Use `openPhotoLightbox` from any component instead of threading the setter. Harness `train/v479.mjs` (admin 365582 → History → PANTRY card → ✏️ Edit → `__sdxJumpToGuideItem({pid:1, key:'facility.ceiling', ci:1})` renders the strip): preview photo opens its lightbox on a real pointer click, the strip thumb opens the app viewer with the Storage URL, tap-outside and ✕ close it, 0 page errors. NOTE: `sdx-open-guide-item` alone leaves the panel hidden; the jump helper switches the step.
- v478: Samantha's PANTRY #342 report filled in + the license box can never hold a stand name + bigger crew chips. Joxel: "can you please fill up the information correctly for this report that samantha did" / "there are many pics without comment and also no place" / "i want to feel more separation between maintenance, cleaning and ecolab maybe make the buttons bigger". (a) Record `inspections/1789746923528_yxdl15`: `restaurantLicense` was `"PANTRY "` (typed into the license box, then REMEMBERED by autofill memory), 31 photos but only 5 rows commented, every `ciLocation` was `"."` and every `corrective` `"report"`, 12 photographed rows never marked NO, `overallStatus` Pass. The corrected record is built LOCALLY by scratchpad `sam/build.py` from the downloaded raw doc (`/tmp/claude-0/rec.json` → `rec_patched.json` + `rec_patched_plain.json`): 13 checklist rows → NO + comment (what the photo shows) + ciLocation + corrective + ciStatus (Maintenance for holes / broken drain cover / soap dispenser), the two custom cooler nodes get `kitchenArea` + `corrective` + the visible USE BY 02/24/26, `actionItems` rebuilt the way `buildActionItems` does (15 items, `issue "Facilities – <SECTION_LABELS>: <problem> [<place>]"`), license "", `overallStatus "Needs Attention"`. WRITTEN 2026-09-22 ~22:20Z by scratchpad `sam/send.py` (REST PATCH, `updateMask.fieldPaths` = restaurantLicense, licenseMissing, overallStatus, inspection, actionItems, with a `currentDocument.updateTime` precondition; rules allow unauthenticated update when `validInspection()` holds). Two rounds of independent verifiers (Workflow; lenses facts-vs-photos / data shape / rules) caught real errors in my first draft: counts and places the photos could not prove, three findings on the wrong checklist row (grille ≠ sprinklers, debris ≠ cracked tile, mop-sink basin ≠ mop-area floor → photos moved to Vent covers / Under equipment / Utility sink), her wording rewritten instead of extended, and `kitchenArea` on custom nodes (it changes the app's label formula — never add it). Between download and write the record was re-saved from the form (22:07Z: walls / ceiling[2] / ceiling[6] / handSink[1] got human comments, one hand-sink photo removed → 30 photos); the precondition refused the first write, `fetch.py` diffs versions and `build.py` was rebased: human text kept verbatim + extended, only `.` / `report` / `chilling` placeholders replaced. LESSON: a report someone has open in the form overwrites a REST correction on their next Save — tell them to reopen it from History first. Unit 342 on the license sheet is Mr Chory (portable) but Joxel confirmed: "samantha checks bars so is nothing like mr chory portable subcontractor" — the PANTRY #342 / Floor 3 / Pantry identity stays as she wrote it; do not remap bar/pantry reports to the license-sheet stand at the same unit. (b) App: `looksLikeLicense(v)` (4+ digits, or NO LICENSE) + `isLicenseExemptType` next to `lookupLicenseByUnitType`; `learnFromSave` stores a license only when it looks like one; `applySiteAutofill` and the QR-prefill `last.restaurantLicense` apply only real ones; `scrubAutofillLicenses()` runs once at load and blanks remembered non-licenses; the save block writes `restaurantLicense ""` for Bar / Pantry and drops non-license text otherwise; the form shows `.licExemptNote` "License not required for a Pantry" (`data-testid="lic-exempt"`, no input, no Remembered badge) for Bar / Pantry and `.licNotNumber` "is not a license number — it will not be saved" while a non-license string is typed. (c) Follow-ups summary bar: the cleaning / maintenance / Ecolab chips sit in `.fuCrewGroup` (`data-testid="fu-crew-group"`, 14 px gap + 2 px left rule; on phones a full-width row under a top rule) as `.fuCrewChip` (0.9 rem, 800, 44 px tall, 2 px border). Harness `train/v478.mjs` (v477 head + the patched record seeded into `sdx_history_cache_default`): History card = 15 issue rows incl. both coolers, 13 with [place] + 🔧 (the two custom-node rows come from the inspection tree, which prints no corrective), no "[.]" / "report"; Follow-ups PANTRY #342 group + 3 crew chips ≥ 40 px, desktop gap 20 px; memory scrub; Pantry → exempt note; Concession + "PANTRY" → warning, NOS → none. ALL PASS.
- v477: adding a photo to a report no longer freezes or crashes the tab, and nothing is skipped silently. Joxel: "sometimes when i click to load a picture from a report it just dont let me and it crashes" (Mac/iPad Safari; the 📂 / 📷 buttons on checklist rows and the BEFORE / AFTER buttons). ROOT CAUSES: (1) `addCiPhoto` (~25000) and `processPhotoFiles` (~24546) read the ORIGINAL file with `readAsDataURL` and kept that ~11 MB base64 string in React state as `exportUrl` / `previewUrl` (and the thumb strip rendered `previewUrl`), six per item, cloned on every `setInspection`; the 4 s draft autosave then `JSON.stringify`-ed the whole inspection with those strings into localStorage (only the cloud draft was slimmed) → memory spike → Safari killed the tab. (2) "doesn't let me": files with an empty `type` (iPad HEIC), files over the old 8 MB cap (a real camera photo is 8–12 MB), and anything `compressImage` could not decode were dropped with NO message. Now: `prepPhoto(file)` (next to `compressImage`) decodes ONCE (`createImageBitmap` with `imageOrientation: "from-image"`, `<img>` fallback, object URLs revoked, bitmap closed) → 220 px thumb + 1600 px q0.85 export (~150–600 KB); accepts empty-type files by extension; cap 25 MB (we resize anyway); reasons `not-image` / `too-big` / `decode-failed` → `photoSkipToast` through the existing `onError` ("📷 1 photo skipped: notes.jpg could not be read — try the camera button"). Both adders process files SEQUENTIALLY, upload the export, and after a successful upload store the Storage URL as BOTH `previewUrl` and `exportUrl` (exports fetch it through `fetchAsDataUrl`); the resized data URL stays only as the offline fallback. The strip `<img>` renders `thumbUrl` (lightbox keeps `previewUrl`). `saveDraft` (local) goes through `slimLocalDraft` (drops `data:` strings > 60 KB and File/Blob values, keeps thumbs). `.fileInput` is visually hidden instead of `display:none` (Safari sometimes ignores `.click()` on a display:none file input). Saved records were already safe (31293 strips data URLs). Harness `train/v477.mjs` (v476 head): fryer node opened by full key, row 0 → `.clBtnFail`, `setInputFiles` with an in-page 4000×3000 JPEG, the same bytes as `IMG_0002.HEIC` with EMPTY mimeType, and a text file named .jpg → 2 thumbs (< 60 KB src each), the fake file's toast, local draft 45 KB with thumbs and no data URL > 60 KB; v476 / v459 pass.
- v476: "Go fix →" lands on the exact checklist row, and Subcontractor / Kitchen / Portables get the right equipment list. Joxel (pre-submit modal screenshot): "This should sent me directly to the problem when I click it and also make all of my inspection guide better I do not have hoods and fryers warmers and all of that to fill up subcontractors and concession have them just portable that we have to fill up". ROOT CAUSE of the dead jump: guide anchors are `data-guide-key={it.path.join(".")}` ("equipment.fryer") but the proof gate (31135) passed the bare node key ("fryer"), so `jumpToGuideItem` only switched the step and never found an element. Now the gate passes `key: \`${sec}.${key}\``, `ci` (row index) and `need` (photo | action); each `.checklistItem` carries `data-cl-idx`; `jumpToGuideItem` polls up to 2 s for `[data-guide-key] [data-cl-idx]` after the node opens, centres and flashes the ROW, focuses its corrective textarea when the action is what is missing, and reports `window.__sdxLastJump`; `window.__sdxJumpToGuideItem` is exposed for harnesses. Modal copy no longer mentions "why there is none" (stale since v454). Lists: `FULL_EQUIP_ITEMS` (the old Concession array) now serves Concession, Subcontractor AND Kitchen; `isPortableType` → `PORTABLE_EQUIP_ITEMS` + `PORTABLE_COLD_EQUIPMENT` (`cartCooler`, `cartFreezer`, `iceChest`; rows: hot holding, sneeze guard, hand-wash station, sanitizer bucket + strips, thermometers, cart surfaces / wheels / canopy, power / generator / propane, trash, license posted, other) — no fryer / hood / grill for carts and no empty add-your-own hint; Bar / Pantry (v475) and Event / Temporary unchanged; `coldMapGet` and the prediction label / escalation maps know the cart keys. Harness `train/v476.mjs` (v475 head): Subcontractor + Kitchen show Fryer / Hood / Warmers; both Portable types show the cart rows with a ❄ badge and no fryer; Concession + Bar unchanged; jump test = force the equipment panel visible, `sdx-open-guide-item` with `equipment.fryer`, then `__sdxJumpToGuideItem({pid:2, key:'equipment.fryer', ci:last, need:'action'})` → `__sdxLastJump.found`, the row has `guideFindFlash`, its centre within 30 % of the viewport middle; v475 / v459 pass. NOTE: this is the first time a checklist ROW was reached headlessly — opening the node with `sdx-open-guide-item` using the FULL path key renders its rows (the v439/v445/v450 failures used the bare key).
- v475: Bars and Pantries have their own equipment checklist. Joxel: "when i select bar or pantries they have their own stuff that i have to fill in like soda machines and all of the that they have". Before: the Bar branch (form ~32740) had coolers / ice / glass washer / beer lines but nothing about soda or drink service; Pantry fell into the generic `Equipments — ${locationType}` empty list ("Tap + Add Item"). Now: Bar gains `sodaMachine` (Soda Gun / Fountain), `co2Tanks`, `speedRails`, `blender`, `garnishCooler` (cold, in `BAR_COLD_EQUIPMENT`), `dumpSink`, `liquorStorage`, `glassStorage`; a new `locationType === "Pantry"` branch with `PANTRY_COLD_EQUIPMENT` (`reachInCooler`, `reachInFreezer`, `milkCooler`) and rows Reach-In Cooler / Freezer, Milk / Dairy, Soda / Fountain, Coffee / Tea brewers & airpots, dispensers, microwave / toaster / panini, ice machine, warmers, dry storage (6" / FIFO), Ecolab, other. Keys need NO template entry — `GuideSection` creates nodes on demand (the bar keys never were in `buildDefaultInspection`). `coldMapGet(k)` (next to the maps) replaces every `COLD_EQUIPMENT[k] || BAR_COLD_EQUIPMENT[k]` lookup (8 sites incl. the multi-line one in the equipment page ~20805) so pantry units get ❄ badges, temp boxes, zones and follow-ups; `detectChecklistKey` maps soda / coffee / blender words; `ITEM_LABEL` + escalation `SECTION_MAP.equipment` in `buildPredictions` know the new keys. Kitchen / Subcontractor / Portable / Event keep the add-your-own list. Harness `train/v475.mjs` (v459 head; `pick(type)` sets the type `<select>` through the native setter + change; the equipment panel is `[data-guide-panel="2"]`, read it with innerText even while `display:none` — it is UPPER-CASED by CSS, match `/i`): Bar rows + kept rows, Pantry rows with ❄ badges and no empty hint, Concession unchanged, Kitchen still generic; v459 and v474 pass. Temp inputs sit inside collapsed items, so count badges, not inputs.
- v474: Predictive Insights say WHAT, WHEN, WHO HAS IT and WHAT NEXT. Joxel (iPad screenshot of 15 medium-risk cards all reading "'Facilities – Floor' issues have appeared in 2 of the last 3 inspections at Lucky Chicken (67%)" / "hasn't been inspected in 43 days and had 2 unresolved issue(s)"): "I want the feedback to be more accurate is missing information". ROOT CAUSES in `buildPredictions` (7065): stands grouped by NAME ONLY (three #114 stands merged, one name at two units merged); quick reports / supervisor logs counted as inspections; recurrence showed only the category — no dates, no item text, no notes, no photo, no crew status; "unresolved" ignored `followupCleared` / `followupStatus` / `rec.resolvedIssues`, so fixed items still counted; temp drift read five legacy keys (`doubleDoorCooler`…) instead of the stand's real units; every detail was the same sentence. Now `buildPredictions(history, venueSettings)`: stands keyed `normUnit|standSlug` and labelled `NAME · #unit`; `isVisit` (not quickProblem/supervisorLog/quickEquipCheck) is the base — stand reports become `+ N reported by the stand`; each prediction carries `evidence[]` (date · inspector · item text after the colon / area / notes, 📷 when a photo exists — `itemText`), `status` (`statusOf(locName, cat, lastTs)`: ✅ Closed by NAME on date / 🔧 In process since / ⏳ Waiting on / ↩ Put back / ⚠ Still open), `detail` = a category-specific next step (`nextStep`) and `crew` (`classifyIssueType`); recurrence risk is high when ≥85 % or open ≥3 times; temp drift uses `collectEquipTemps` per unit (`assetTag || key`; rising 3 or over the max twice) with brand · area · tag and each reading dated; overdue lists only issues still open (skips the card when all are closed) plus "N fixed since · N supervisor log(s) since"; hand/3-comp sink read `inspection.temps` with dates. Panel (`PredictiveInsightsPanel({history, venueSettings})`, wired at the analytics switch) shows a scope line (`N inspections at M stands (from – to), plus K quick reports / supervisor logs as evidence`) and an expanded card = WHAT WAS WRITTEN EACH TIME list + status line + 💡 next step · crew (`.predEvidence*`, `.predStatus`, `.predNext`, `.predCrew`). Harness `train/v474.mjs` (v455 head, own seeds): LUCKY CHICKEN #212 2-of-3 + 1 quick report, evidence rows with notes and 📷, status In process — ANA R., next step names Cleaning; OLD STAND #300 shows `1 of 2 issue(s) … Plumbing` (the cleared Cleaning one is excluded); COLD STAND #118 walk-in DELFIELD 34 → 37 → 39.5 °F climbing, high risk; v473 passes.
- v473: the bottom of every follow-up card is one action bar. Joxel: "The bottom of resolve for the follow ups make it better and make everything good test everything of the app". Before: `.fuActions` (two 0.7 rem pills `🔔 Remind` / `✓ Resolved` — the label said Resolved on an OPEN item) wrapped under the card on phones, beneath a loose `.fuPhotoRow` (3 photo labels) and a `💬 Add comment` text link — three unrelated control rows. Now the card (`.fuItem` is `flex-wrap: wrap` on all sizes) ends with `.fuCardBar` (full width, top hairline): `.fuBarSecondary` = `🔔 Remind` (`.fuBarRemind`, `.on` = ✓ Added), `💬 Comment` (toggles the existing `commentKey` input — the text link is gone), `📷 Photo` (`photoMenuKey` state → `.fuPhotoMenu` popover with the same three inputs: 🔴 Before · camera / 🟢 After · camera / 🖼 Upload; closes after a pick); primary `.fuBtnPrimary` `✓ Resolve` (green, 44 px, min 130 px, `✓ Cleared` + `.done` for the 9 s undo window). Phones ≤ 560 px: secondary row stretched, Resolve full width underneath. Group heads (`Resolve all` / `Remind Team`) and the ✅ Fixed `↩ Put back` rows untouched; dark theme rules added. Harness `train/v473.mjs` (v455 head; runs at 390 and 1100): bar is the card's last child, no `.fuPhotoRow` / Add-comment link, Resolve ≥ 44 px and card-wide on phone / right-aligned on desktop, Comment → input → Post shows on the card, Photo → 3 file inputs, Remind → ✓ Added, Resolve removes the card + Undo strip + Undo restores. Screenshots `v473/card-390.png` / `card-1100.png` (remove `.swUpdateBanner` before shooting — the harness triggers it). FULL REGRESSION: every `train/v4*.mjs` (v428–v473, 47 scripts) run in a loop → `train/regress/regress-v473.txt`: 44 green first pass; v455 / v462 / v436 only failed on the old selectors (`.fuItem .fuBtnResolve` → `.fuBtnPrimary`, `.fuItem .fuBtnRemind` → `.fuBarRemind`; group-head buttons keep `.fuBtn*`), v454's quick-report flow had drifted since v458 (it tapped chips that are now behind "Add details", so the block was the few-words rule, not the photo gate — it now types the description). All 47 pass. No app bugs found.
- v472: "Shawarma", not "Shawarma Gyros". Joxel: "is not shawarma gyros it is just shawarma". `LICENSE_REGISTRY` P114 NOS2334412 and P322 NOS2334405 → `Shawarma / Sub` (the `/ Sub` keeps Portable - Subcontractor); `HARD_ROCK_LICENSE_SEED` gained `P114/P322 - Shawarma / Sub` + `P114/P322 Shawarma` keys (old Gyros keys kept). Stand id at #114 moved `u:114~shawarmagyrossub` → `u:114~shawarmasub` (#322 stays `u:322`): `STAND_SLUG_ALIASES` (next to `normUnit`) makes `standSlug("SHAWARMA GYROS / SUB")` = `shawarmasub`, so `standIdIn`/`sameStandName` treat old and new names as the same stand; `canonStandId(id)` / `canonKeys(obj)` rewrite stored ids in `equipBelongsTo` (item.standId), `loadStandListNow` (kitchenRegistry `~slug` keys and their `contacts`), the equipment page's `verified` map and the tracker's `regContacts`. Follow-up keys keep the report's stored name (history). Harness `train/v472.mjs`: stands `u:114~shawarmasub` / `u:322` read SHAWARMA / SUB with no GYROS; a registry unit stored with the old standId sits under SHAWARMA / SUB #114 (not Unassigned); an old poster link with the GYROS name still lists that cart's cooler; v464 and v471 pass.
- v471: the HACCP tracker is ONE list — each stand with its people underneath. Joxel (Analytics → Temps): "I want to merge by stand with by person". `HaccpTodayTracker` lost the `view` toggle; the universe is now the REAL stand list (`_standListCache` from `loadStandList()` — unit + stand name, so the three stands at #114 are separate rows; `buildHaccpDirectory`'s unit-keyed `stands` merged siblings and put LA PLACITA's supervisor into "no stand"), plus any `dir.stands` id not matched by unit+`sameStandName`. Per stand: `subFor(k, r)` picks its submissions → `lastAt / checksOnDate / doneBy / submittedToday` (temps > 0 on `day`); people = `standPeople(k, subs)` (v468: logs + manual contacts from `kitchenRegistry.items[id].contacts` — `regStands` now keeps `rid/floor/locType/contacts`) merged by `phoneDigits` with `dir.people` (`submittedToday`, `activeOnDate`, override name; `pkey` = the directory's phone form so `saveTeam` edit/remove/restore keep working); `dir.hiddenPeople` stays as "Removed (N)"; people whose logs match no stand go in a last "People with no stand on file" row. `.haccpStandRow` (head: name + `<StandType>`, "N checks this day / last log / never logged · nobody on file", 👷 pill, 🕵 pill via `inspFor` (id or unit), 📣 Request temps) + `.haccpPersonRow` per person (● on shift, name, phone, role, `✓ Logged HH:MM` / `⏰ Missed` / `—`, `💬 Text` = `smsHref(phone, standInviteText(k, name, p))` — the personal signed-in link replaced the generic reminder, ✎, ✕). Chips: logged / missed / All / `👤 pending` and `🕵 inspected` are now clickable filters (`standFilter` "pending" | "inspected"); order `standPrintOrder`, logged last under All. `📨 Text all missed (N)` opens `TextStepper` (new top-level component extracted from v470's directory stepper — `{list:[{k,p}], onClose, title}`; the People directory uses it too). Harness `train/v471.mjs` (desktop 1100 px; seeds MARIA temps at MAGIC CITY DOGS #114, LUIS ident-only at LA PLACITA #114): no toggle, MCD #114 row ✓ Logged with Maria, LA PLACITA missed with Luis + Text (`&n=LUIS`), missed chip, stepper lists only Luis, ✕ → Removed (1) → Restore. NOTE: four stands are named MAGIC CITY DOGS — match rows by `NAME · #unit`. Counts now cover every stand with a poster, so "All" is larger than the old kitchenRegistry-only number.
- v470: supervisors keep their own equipment list right, can file several problems, and get personal links. Joxel: "I want the supervisor to be able to add or remove equipment because maybe they change the location of it", "let's find the best way to send them all a text message with the links of their stand", "I want to also let the supervisor put more than one issue" (screenshot: chip picked, editor empty, no Add-another in sight — v463's button only rendered once the editor had content), "I want them already connected to their stand … when I'm going to send them the message". (a) Portal equipment card: each row gets `✎ Edit this unit` (`.htEqMore`, tap first — the three actions do not exist until then) → `Fix name / location` (inline form), `Moved to another stand` (search via `loadStandList()`, tap a stand → record gets its site/unit/floor/type/standId), `Not here anymore` (confirm → `venueName "UNASSIGNED", unit "-", removedFrom` so it lands in the inspector's Unassigned bucket — an empty unit would NOT override the report snapshot); `＋ Add a cooler / freezer that is missing` (name chips `PORTAL_UNIT_NAMES` or typed, brand, location → next free `SDX-CL|FZ-<unit>-<n>`, written with `assetTag`). All through `eqWrite` → `writeEquipReg({items, labelIndex})` (sharedMemory writes are public) stamped `changedBy/changedPhone/changedAt/changeNote`; the inspector's `renderWalkRow` shows `.walkChanged` "✋ note · NAME · date" for 45 days. (b) `.supAddAnotherRow` is always visible under the corrective action; tapping with an incomplete editor explains what to fill in. (c) `standInviteUrl(k, person)` appends `&n=NAME&p=PHONE`; `HaccpPortal` reads them (`urlName/urlPhone`, `autoConnect`) and jumps to `step "form"` already signed in, writing the `ident` record with `viaLink: true`; `.haccpNotYou` "not you?" goes back. (d) People directory `📨 Text everyone` = a stepper (`bulk`) one PERSON at a time (each link is theirs): ✉ Open text (`sms:`), 💬 WhatsApp, 📋 Copy, Next/Skip, Done. A real one-tap blast needs an SMS service (Twilio-type account) — not built. Harness `train/v470.mjs` (ALL PASS; the seed already had `SDX-FZ-114-1`, so a new freezer takes `-2`; the name inside the sms body is URL-encoded — match `MARIA(%20| )P`); v463/v468/v469 pass.
- v469: the stand people get the Food Safety Quick Reference. Joxel: "They should also have the food safety quick reference as well". `FoodSafetyRef` (15353 area) now takes `{ lang, portal, defaultOpen, headerOffset }`; every label / row / tip goes through `t(en)` = `FSR_ES[en] || en` (the dictionary `FSR_ES` sits right above the component — ~120 strings: title, six tabs, zones, cook temps, calibration, sanitizers, cutting boards, cooler shelf order, FIFO, label fields, day dots, shelf lives, thawing methods, "Never do this", tips; the rich `<strong>` tips became plain `t("…")` strings). The stand portal mounts it twice: `.haccpRefWrap` above the temperature form (`step === "form"`, collapsed, `portal` variant `.fsrPortal`, headerOffset 8) and on the Submitted! screen open with the lead "While you're here — …". The inspector form's instance is untouched (English, sticky). `standInviteText` gained the line "The link also has the Food Safety Quick Reference (…) · También tiene la referencia rápida…". Harness `train/v469.mjs`: card present, collapsed above `.htFoodHead`, six tabs + 135°F, Español → title/tabs/ZONA DE PELIGRO, Descongelar panel (innerText is upper-cased by CSS — match `/i`), coolers focus intact, done screen open, inspector form still one English instance; v468 passes. NOTE: `v461.mjs` selected its card by a hard-coded date — it drifts daily; it now uses `dayAgo(2)`.
- v468: the people at each stand, and a link that asks for cooler temps. Joxel: "Let's have the record of the people in a stand (the phone number and name because I want to send them the link of their stand) and I want them to check cooler temperatures also". `standPeople(k, subs)` (next to `standHaccpUrl`) = everyone who filed a HACCP log at that stand (name + phone from `haccpSubmissions`, newest name wins, `logs` count; matched by unit AND `sameStandName` when both sides name the stand — three stands share #114) plus the inspector's own `contacts` on `kitchenRegistry.items[id].contacts` `[{name, phone, role, addedAt}]` (`loadStandListNow` carries `contacts` onto registry AND seed stands; `saveKitchenEdit` keeps them). Posters tab: every card gets `.kqrPeople` — "👥 People at this stand (N)", rows with `✉ Text link` (`smsHref`: `sms:+1…?&body=`), `💬` WhatsApp (`waHref`: `wa.me/1…?text=`), `📋` copy, ✕ for manual people, and `＋ Add person` (name / phone / role → `savePerson` writes the registry item with `contacts`); header `👥 People` opens a directory modal (search, per-stand groups in `standPrintOrder`, `📋 Copy list`). The message (`standInviteText`, EN + ES) carries `standInviteUrl(k)` = the poster URL + `&coolers=1`; the portal reads `urlCoolers` and, on the temperature form, scrolls to `.htEquipCard`, outlines it (`.htEquipFocus`) and shows `.htEquipAsk` "The inspector asked you to log EVERY cooler & freezer temperature today". Harness `train/v468.mjs`: seeded log → card row + sms/wa hrefs (number, unit, coolers=1, EN/ES cooler wording), add person → 2 people on the card and on `window.__sdxStands`, directory `2 people · 1 stands`, search; portal with `coolers=1` → highlighted card, banner, scrolled to top. Local mode: `writeSharedDoc` is a no-op, so assert on state, not the outbox.
- v467: the app reads and understands what people type — everywhere. Joxel: "I want the system to be able to read and understand what the user is writing" (confirmed: supervisor problem text, inspector notes, and every other box). Rule-based, offline, EN/ES (Kreyòl for the crew words) — NO model, no network. Engine `understandText(text, {units, lang})` right after `specCatKey` → `{lang, category, crewType, severity, unit, area, temps[], details{}, corrective, mentions{floors,types,units}, matched[]}`; tables `NLU_CATS` (ordered: Pest → Ecolab → Lights → Plumbing → Equipment → Temperature → Cleaning → Maintenance; "grease under the fryer" stays Cleaning because an equipment noun only wins with an `EQUIP_FAULT` word), `NLU_URGENT`/`NLU_INFO`, `NLU_AREAS` (canonical "where" chips), `NLU_OPT_SYN` (synonyms per `SPEC_ROWS` option; the most specific option wins — "Hand sink" over "Sink"), `NLU_CORRECTIVE`, `NLU_TYPE_WORDS`; temps need a lead word (at / a / reads / marca / temp) or an explicit °/F/C/grados, never after `unit|#|floor|door`, Celsius converted; the unit is scored against the stand's own list (tag > brand > name tokens > "N-door" > type word > location). `understoodChips(r, lang)` builds the "Understood:" strip; `understandNotes(text)` splits sentences → issues, skipping `Action:`/`Supervisor:`-style lines and pure "we fixed it" lines; rejected sentences persist in `sdx_nlu_rejected_<venue>` (`nluReject`/`nluUnreject`). Exposed as `window.__sdxUnderstand` for harnesses. Wiring: (1) portal editor — 500 ms debounce + blur → `applyProblemNlu` fills `problemCat`/`severity`/`problemDetails` only for fields not set by hand (`nluTouched` ref: the category chips and severity buttons mark it), `.nluStrip` under the textarea with ✕ per chip (`rejectProblemNlu`) and `＋ Log 50°F · UNIT` (`logUnderstoodTemp` writes the reading into that `customItems` row's `temps`, or asks to pick the unit); (2) quick report — same with `qpCat`/`qpDetails` (`qpTouched`, `rejectQpNlu`); NOTE this means two UNDERSTOOD words ("Sink broken") now pass `saidEnough` because the chips are auto-filled — v458's harness was switched to "hmm ok" for the still-blocked case; (3) inspection form — `buildActionItems` walks `rawNotes` and every `node.notes` through `understandNotes` and adds `understood: true` items (`issue: "Category: sentence"`, `area`, `corrective`, priority High when urgent) deduped against existing items; the form shows `.nluNotesPanel` ("🧠 Understood from your notes") with `✕ Not an issue` / `↩ Keep` (`nluTick` re-memo); `extractFieldsFromNotes` also accepts `encargado|gerente|jefe` and `licencia`; (4) `classifyIssueType` gained the Spanish synonyms (pest/ecolab/maintenance/cleaning/temperature) so crew routing, follow-ups and exports understand Spanish records already saved; (5) crew ✅ Fixed box — "ya estaba limpio / already clean" pre-picks 👍, "lo limpiamos / we cleaned it" pre-picks 🧹 unless a button was tapped (`howTouched`); (6) announcements — typing pre-ticks floors / stand types / stands from the text (`mentions`, "every stand / a todos" → all) until a chip is tapped (`targetsTouched`), with a "understood from your message — tap to change" hint. NOT done: pre-selecting the section 🎯 picker from understood chips. Harness `train/v467.mjs`: 20-sentence EN/ES table via `window.__sdxUnderstand` (all pass), portal strip + auto category + "+ Log 50°F" landing on the 2-DOOR COOLER row (seed `sdx_equip_registry_default` = `regdoc.items` or the portal has no units), hand-picked category never overridden, quick report → Plumbing, notes panel with Keep/✕, announcement chips (label text is upper-cased by CSS — match `/i`), crew box pre-picks; v458/v463/v466 pass.
- v466: a supervisor log is a report of its own. Joxel: "I want for the supervisor that they can create their own report because sometimes they have sent me information and I end up not getting it because I haven't created a report". ROOT CAUSE: a stand-poster scan writes only `haccpSubmissions` (no `reportId`); Past Reports shows only `inspections`, and a submission attached to a card only by `reportId` or same day + same stand WHEN an inspector report existed — no report that day → the temps lived only in Analytics → Temps. Only the problem path created a record (Quick Report). Now `HaccpPortal.handleSubmit` files ONE `inspections` record for every submission with temperatures (`haccpTempCount(record) > 0`): `inspectionType: "Supervisor Log"`, `supervisorLog: true`, `quickProblem: false`, `source: "haccp_portal"`, `haccpSubmissionId`, `haccpTempCount`, `haccpOutOfRange` (via `tempPass` over `HACCP_TEMP_ITEMS` + `customItems`), `restaurantLicense` from `lookupLicenseByUnitType`, `supervisorName`/`sitePhone`/`reportedBy`, `overallStatus` FAIL when any reading is out of range, problems as its `actionItems` (so a temps+problems visit is one card, not two); problem-only submissions keep the Quick Report path. The HACCP record keeps NO `reportId`, so it still attaches to the inspector's same-day report; both matchers (`haccpStatusForRecord`, `haccpSubsForRecord`) gain `s.id === rec.haccpSubmissionId` so the log's own card shows the ✓ pill and the "HACCP Temperature Logs" section (the expanded-card loader `loadHaccpBySite` already finds it: same day/unit/no reportId). History card: `.supLogBadge` "📋 SUPERVISOR LOG · by NAME · N temps logged · N out of range · N problems · 📞", the problem block renders for logs with problems too, and `uniqueTypes` offers "Supervisor Log" in the type filter automatically. `supervisorLog` is excluded wherever `quickProblem` was (latest-inspection rollup 8454, follow-up inspector name, the equipment page `sortedRecs`, stand-QR prefill 29650). `notifySupervisorLog` (next to `haccpTempCount`) sends `saveInspectorNotification` to approved inspector/admin/global_admin users (best effort). Harness `train/v466.mjs`: portal → tap an item button (🔥Hot Holding / 🐔Poultry) to reveal `.haccpTempInput` → two temps → Submitted!; seeded log + submission + same-day inspector report → badge text, BOTH cards show `HACCP ✓ 2 temps`, filter has Supervisor Log, expanded card lists 120°F / 170°F (wait for the 4 s local poll; `innerText` is upper-cased by CSS — match `/i`). NOTE: in local mode the portal's `saveOneInspection` cannot be observed (no crypto key) — the record shape is proven by seed.
- v465: Equipment & verify walk no longer sits on "Loading equipment…". Joxel (desktop screenshot of the tab stuck on the spinner): "this takes too long to show the information". ROOT CAUSES in `PrintLabelsPage`'s load effect: (1) it ran TWICE — the first run derived the cutoff from the registry and called `setCutoffDate`, which is in the effect's deps; (2) everything was awaited in series: `flushOutbox` → `getDoc(equipmentRegistry)` (~800 KB) → `loadHistory` (300 records) → `loadStandList()` (kitchenRegistry + ANOTHER 300-record `loadHistory`) → `warmEquipRegistry()` re-downloading the registry; so ≈4 registry reads + 4 history pages per open; (3) nothing painted from the local copies. Now: `skipNextLoadRef` swallows the re-run the cutoff write causes; the registry `getDoc` and `loadStandList()` run in `Promise.all` (getDoc wrapped in `Promise.resolve().then` — in local mode it throws synchronously and used to skip the stand-list call), then one history read; the snapshot itself feeds `_equipRegCache` / `indexEquipLabels` (no `warmEquipRegistry`); the item-building code moved verbatim into `build({regItems, regIndex, hidden, cutoffMs, cutoffMode, recList})` inside the effect and runs FIRST from `sdx_equip_reg_doc_<venue>` (now WRITTEN on every online load: items/labelIndex/hidden/setup/verified/confirmed/cutoff) + `sdx_history_cache_<venue>` — `setLoading(false)` immediately, `.equipRefreshChip` "↻ Updating from the cloud…" until the live build lands, then "✓ Loaded in 2.4 s" for 6 s and `console.info("[equip] cache · registry · stands · reports · total")` (`window.__equipLoadStats`). `loadStandList(opts)` is memoized 60 s (`_standListLoadedAt`, in-flight `_standListPending`, `{fresh:true}` bypasses; posters add/remove/restore patch `_standListCache` so the other tab sees them); `loadStandListNow` is the old body. Harness `train/v465.mjs` (v441 seeds): list painted 77 ms after nav, `window.__equipLoads` 1 (was 2), stand list loaded once across a posters↔equipment tab switch, a date pick reloads once, 0 errors; v464 poster harness still passes. Cloud timings cannot be measured here — his next screenshot/console shows them.
- v464: the 2026 license sheet reconciled, and posters print in unit order divided by floor. Joxel pasted the full sheet ("make sure we have all of the information right … print the qr codes by order and divide them"; P = portable, C = concession, S = subcontractor). Diff (scratchpad `train/sheet2026.txt` vs `LICENSE_REGISTRY`, node eval of the source block): 106 rows, 0 mismatches on unit/type/license/status after two fixes — the G kitchen `NOS2334854` had lost its "72" (`72 Club South/East`; `cleanStandName` keeps it because 72 ≠ unit G), and the 354 Hot Dog note pointed at the 325 cart. Counts: 89 ACTIVE, 12 NEEDED, 4 REQUESTED, 1 file-ref. Deliberate differences kept: spelling fixes, unit prefixes stripped from kitchen names, P122 Arepa stored at 119. Printing: `standPrintOrder` (next to `standSeeds`) = `floorRank` (Floor 1→2→3→Ground→other→none) → `normUnit` numeric → C/K/S/P → name; `printPosters` sorts with it, `standPosterHtml` groups by `floorLabelOf` and emits a `.pdiv` divider sheet per floor (big FLOOR N, count + unit range, one pill per stand, "Sheet i of n"; `break-before/after: page`, first one no break-before) so the stack splits at the printer; the on-screen grid renders in the same order with `.kqrFloorHead` rows (`gridColumn: 1 / -1`). Harness `train/v464.mjs`: stubs `window.open` to capture the print HTML — dividers `Floor 1:46 | Floor 2:16 | Floor 3:40 | Ground Level:3 | No floor:1`, every poster ascending by unit, the Floor 2 pill alone prints 16 posters with one divider; `posters.pdf` / `print-page1.png` eyeballed.
- v463: the supervisor log takes several problems, coolers sit under the food temps, and Submit is instant. Joxel: "i want the supervisor log to be able to report more than one issue and i want the equipment temp to be under the food temperature. and the submit button does not work correctly or it is too slow". (a) Multi: `problemsAdded[]` + the existing editor = the next problem; `validateProblemEditor()` (category / `saidEnough` / `proofGate`) is shared by `addAnotherProblem()` (`.supAddAnotherBtn`, shows once the editor has content; pushes `editorToProblem()`, clears the editor, scrolls to the chips) and `handleSubmit`, which validates the editor only when it has content and files `allProblems`. Record: `problemReports: [...]` (one per problem) PLUS the old single `problemReport` as a MERGE (texts joined ` | `, worst severity, all photos) so the ~12 existing readers (exports, History, HACCP tracker) keep working; ONE Quick Report inspection with N `actionItems` (each with `problemReportId`) — one follow-up per `loc::cat` as before — and one `saveProblemReport` per problem. Added problems render as `.supAddedRow` chips (`✕` removes), the button reads `Send N Problem Reports`, the done box lists every category. (b) Order: the `htEquipCard` block moved AFTER `afterCooking` (below the FOOD TEMPERATURES section); hint copy now says the equipment is listed underneath. (c) Submit: ROOT CAUSES — the validation error rendered only inside the problem section, far above the button (tap → "nothing happens"), and the three Firestore writes were awaited in series before "done". Now the error also shows right above the Submit button with a "go to the problem" link, and only `saveHaccpSubmission` is awaited — the problem reports, the Quick Report record and `notifyCrewsForItems` run in a detached async block (each try-wrapped) after `setStep("done")`. Harness `train/v463.mjs`: card below the food head (y 1753 vs 796), two problems added → `Send 2 Problem Reports` → Submitted! in ~10 ms, `problemReports.length === 2`, merged text names both; validation error appears within 120 px of the button. NOTE: `portalIdent` lands on the location step — tap `.haccpSubmitBtn` ("This is my location") to reach the temperature form, `.haccpProblemOnlyBtn` for the problem-only form.
- v462: the reminder says what the problem IS. Joxel (Quick Check modal "Recheck Needed — Walk-In Cooler — American panel — Back of House … is still open"): "i do not even know the issue". `remindMany` / `remindGroup` printed only `f.cat` (for a temperature item that is just the unit's name) and `checkItems` were `"cat — loc"`. New `fuProblemText(f)` in `RecurringIssuesPanel` = `cat (area when the cat lacks it) — detail`, used in the one-item body, every bullet (basket and stand/type/floor/date groups) and therefore in `notifyAssignedUsers`; `fuCheckItem(f)` = `"cat — loc\ndetail"` so the answer key keeps its old prefix and old announcements render unchanged. The Quick Check modal item shows line 1 bold and `⚠ <detail>` amber underneath; the announcement-card chips show `cat — loc · detail`. Harness `train/v462.mjs` (v445 temperature seed): basket Send → body has `22°F (max 10°F…)` and the stand, `checkItems[0]` has the reading on line 2; `Remind Team` bullets carry it; Messaging → Announcements → Start Quick Check → the modal shows the reading. Nav page key for the inbox is `messaging` (tabs Messages / Alerts / Announcements are plain buttons).
- v461: the notes under every issue line, and the wide Scan bar takes the tap. Joxel (End-of-Day list, the `📷 Scan stand QR — start an inspection…` bar, a saved report's FAIL/✓ Resolve rows): "i want to be able to see all of the notes. i want that button to be clickable to scan". (a) ROOT CAUSE of the dead bar: it lives inside `.foodSafetyRefWrap`, which is `position:sticky; pointer-events:none` so the sticky wrapper never blocks the page; `.foodSafetyRef` opts back in with `pointer-events:auto`, the button never did — every tap fell through it. Fix: `.scanStandBtnWide { pointer-events:auto; position:relative }`. (b) New top-level `<IssueNotes item>` renders `📍 AREA` (only when the issue text does not already carry it), the description (`notes`, skipped when it is just "Corrective action: …") and `🔧 corrective`; rendered under the issue text in the saved-report `.issueRow`, the End-of-Day `.eodItemWarn` (issue wrapped in `.eodItemBody`) and the on-screen report's `.rptActionRow`. Also `filteredMeta` (History, 11313) dropped `area`/`corrective` when mapping quick-report action items — now carried. Harness `train/v461.mjs`: `elementFromPoint` at the bar's centre is the bar (was the page under it), clicking opens `QrScanModal`; a seeded quick report with area/notes/corrective shows all three on the History row (expand a card by clicking its `.cardHeader`, not the card). The End-of-Day list is the same component/data — not driven headlessly (it needs a flagged checklist item after 5 PM).
- v460: the update banner moved to the bottom. Joxel: "the reload because of an update — sometimes it does not let them click reload. put it on the bottom bar so they can click save if they are doing a report". `src/main.jsx` `showUpdateBanner()` (module-level, also `window.__sdxShowUpdateBanner` for the harness) replaces the inline top banner: `.swUpdateBanner` is `position:fixed; bottom:0; z-index:1200` with `env(safe-area-inset-bottom)` padding, and a `MutationObserver` + resize listener keep `style.bottom` = the height of `.stickyActionBar` when the report's View/Saved/New bar is showing, so the banner sits ABOVE it and both stay tappable. Text: "⬆ New version ready — Finish and save what you are doing, then reload. Your draft is saved automatically." with `Later` (dismiss, re-shows after 10 min) and `Reload now` (44 px buttons). Why the old one failed to take the tap: it was fixed at `top:0` under the app's fixed header / the phone notch. Harness `train/v460.mjs`: on the form and with a fake `.stickyActionBar` injected, `elementFromPoint` at the Reload button's centre is the button, the banner's bottom equals the bar's top, the bar's buttons stay hittable, the header is uncovered, Later removes it. A real SW update cannot be triggered in the harness — only the DOM/placement is proven.
- v459: typing never prefills — only a scanned stand QR does. Joxel: "take out the prefill please. it only prefills when you scan the qr code because im having problems with the bars and pantries when im putting them in". The inspection form's Location-name `onChange` called `applySiteAutofill()` on every keystroke (memory `siteMap` + license seed → unit, floor, type, license, phone, supervisor, even a remembered equipment list), and the Unit-number `onChange` wrote the stand name + license from `lookupLicenseByUnitType` / `LICENSE_NAME_BY_NUMBER` / `lookupLicenseFromSeed` — so a bar or pantry sharing a unit (or a name prefix) with a licensed stand got that stand written in. Both inputs are now plain: name → `setSiteName(value.toUpperCase())`, unit → `setSiteNumber` + `floorFromUnit` only (the datalist suggestions and `smartFieldCorrect` on blur stay). `applySiteAutofill` itself is untouched and still runs from `applyStandFromQr` (QR link `as=inspector` and the in-app 📷 Scan) and from the "Your Assigned Locations" chips (a deliberate tap). Harness `train/v459.mjs`: seeds `sdx_autofill_memory_default` with MAGIC CITY DOGS and a `BAR` entry; typing `BAR 232 PANTRY`, `MAGIC CITY DOGS` and unit `114` leaves unit/name/license empty; the `?haccp=1&…&as=inspector` link then fills name, unit 114, Concession and license NOS2319802.
- v458: the chip block is optional and tucked away (quick report + stand portal). Joxel (quick report screenshot, three required chip rows above the description): "i do not want that many options and tap what applies should not be mandatory". `submitQuickProblem` and the portal `handleSubmit` no longer gate on `isSpecific` (that needed every `req` row tapped, or 8+ words WITH a location word); the text rule is now `saidEnough(text, details)` next to `isSpecific` — any chip OR 4+ words — with the error "Say what is wrong and where — a few words is enough" (`qpSaidErr` red border on the quick-report textarea; portal `L(…, "Di qué está mal y dónde — con pocas palabras basta")`). Layout: the description textarea comes first (placeholder "What is wrong and where?"), and `<SpecificsPicker>` renders only after a `.specOptLink` "＋ Add details (optional)" tap (`qpSpecOpen` / `problemSpecOpen`, auto-open when a chip is already set) with the new `optional` prop, which drops every `*` and retitles the head "Details (optional) — tap what applies". Also fixed the quick report's `qpProof` banner that still read "Add a photo (or say why there is none)" — v454 missed it; `dist/` now has 0 hits for "say why there is none". The inspection form's section-level 🎯 pickers (24556/24863) are untouched — they were already opt-in. Photo + corrective action stay required. Harness `train/v458.mjs`: portal and quick report show no `.specPicker` and one link; two words + photo + action are blocked with the "few words" message (no "chips" / "say why" wording); six words file successfully (Submitted! / "Problem filed" flash); the link opens the chips with 0 stars.
- v457: BEFORE / AFTER — two big photo buttons on a flagged checklist item (the request queued since v443: "add a corrective action with the picture like the before and after of the problem"; Joxel: "i want like two big buttons before and after for the pictures"). The fail panel's `📂 Gallery` / `📷 Camera` pair is replaced by `.ciBaRow` with `🔴 BEFORE · the problem` and `🟢 AFTER · once it's fixed` (`.ciBaBtn.ciBaBefore/.ciBaAfter`, solid red/green with the count once a photo is in — `.ciBaHas`); each opens one hidden `<input type=file accept=image/* multiple>` WITHOUT `capture`, so the phone offers camera or library in one tap (refs `fileRefs.current[ciRefKey + "_b"|"_a"]`). `addCiPhoto(idx, files, tag = "")` writes `tag` ("" = before, "after"); `.ciPhotoStrip` thumbs carry a `.ciPhotoTag` BEFORE/AFTER strip and red/green borders. The gate now needs a BEFORE photo (`proofGate` gets `photos.filter(tag !== "after")`; hint `⚠ Add a BEFORE photo of the problem — required`; pre-submit line `a BEFORE photo`) — AFTER is optional. Downstream: `computeFollowups` keeps `tag` when copying report photos; the follow-up card (10491) and the crew card (8874) put report photos with `tag === "after"` in the AFTER column (still `.fuThumbReport` "REPORT" label); the three exports already split by `tag`, so the Before & After Excel puts a report AFTER shot in the AFTER column with no change; the PDF photo index caption is prefixed `BEFORE — ` / `AFTER — `. The compact pass-row photo buttons are unchanged. Harness `train/v457.mjs`: seeds a report whose item carries one untagged + one `tag:"after"` photo object and asserts the crew card and the follow-up card each show 1 report thumb under BEFORE and 1 under AFTER, and the Before & After workbook embeds 2 images at cols 16/17 (exceljs `getImages()`, `range.tl.nativeCol` 15/16). The panel itself still cannot be driven headlessly — verified with `train/ba_preview.html` (the exact markup + the built CSS, screenshot `v457/panel.png`).
- v456: "CONCESSION problem" / "SUBCONTRACTOR problem" instead of "our mess". Joxel (Excel Comments column showing `Fixed (lo limpiamos nosotros, desorden nuestro)`): "put instead of desorden nuestro. concession problem and subcontractor. depending". New top-level `whoseProblemLabel(locType, whose, standType)` → `${standTypeBadge(locType).short} problem`, falling back to the stored `standType`, then to the old `STAND / SUBCONTRACTOR` / `Stadium` words for rows that predate this. The crew `commit` tag is `T(`${standType} problem`, `problema de ${standType}`, `pwoblèm ${standType}`)` and the resolved entry now stores `standType`; the stored `whose` (sub/ours) is unchanged so every bill/cleaning reader still works. All three exports' "Whose mess" columns are now "Whose problem" with the type label (Before & After col 14, Selected follow-ups col 21, cleaning PDF), amber still keyed on `whose === "sub"`. FOUND WHILE TESTING: the crew board only read `f.locType` from the record, so any report without a stamped `locationType` (everything before v448, and the harness seed) produced NO whose/type at all — `detectStandType` (v448, quick report) is hoisted to top-level `detectStandTypeFor(unit, site)` and `commit` falls back to it. Harness `train/v456.mjs`: crew fixes three cards (cards are picked by `.crewItemTitle` category — the stand name is only in the group header, not on the card), asserts the comments read `CONCESSION problem` / `SUBCONTRACTOR problem` with no blame words, `standType` on the entries, and reads the Excel back (exceljs) to find the `Whose problem` column at U with the type labels.
- v455: put a fixed issue back. Joxel (Fixed panel header "YESTERDAY · 75 fixed · Joxel Da Silva"): "can we put an option to put back some issues" — after a Resolve-all, the only way back was the 9 s ↩ Undo. `reopenItems(items, why)` in `RecurringIssuesPanel` is the exact inverse of a resolve for any number of keys in ONE write per field: drops the keys from `clearedLocal`, `setStatusLocal`, `writeMap("followupCleared", {key:0…})` (0 = open in `computeFollowups`) and `writeMap("followupStatus", {key:{status:"open", note, by, ts, reopenedFrom: f.fixedTs}})`, notifies the crews, and arms the `.fuUndoStrip` in `mode:"reopen"` with `✓ Resolve again` (which calls `markResolved` per item). `undoResolve` handles both modes. UI: `reopenBtn(f)` renders `↩ Put back` (`.fuBtnReopen`) on every ✅ Fixed row AND cleaning-log row, replaced in place by an inline `.fuReopenAsk` (`Put back LEMONADE CART #104 — …? [Yes, reopen] [No]`) — no `window.confirm`; in select mode the export bar gains `↩ Put back (N)` for the picked fixed items with the same confirm (`reopenAsk.bulk`). Reopened cards show `.fuPutBackChip` `↩ PUT BACK · by NAME · HH:MM` (read from `stMap[key].status === "open" && reopenedFrom`). Timeline: `status === "open"` entries with `reopenedFrom` become a fourth event type `reopened` (amber, `↩ STAND #unit — Category` + `PUT BACK · by NAME · was closed Sep 17`); the builder now iterates all entries and returns early for non-resolved ones. Harness `train/v455.mjs`: crew fix → Put back → No keeps it → Yes → stored open/cleared 0/reopenedFrom, chip 1→0, row gone, PUT BACK chip on the open card, ✓ Resolve again → 1; then two inspector resolves, select both in Fixed, `Put back (2)` → 1 fixed, two `reopenedFrom` entries, Timeline shows 2 PUT BACK events. A reopened crew fix shows faded in the open list because `likelyResolved` (no inspection since) — pre-existing styling, not a bug.
- v454: the "📷 No photo — why? *" excuse box is gone everywhere. Joxel: "i do not want the 'not photo thy?'" — v451 had removed it from the crew box only; it still showed in the quick report, the checklist NO panel and the stand portal. `proofGate` (8629) no longer accepts `noPhotoReason` — no photo = blocked, full stop — and `noPhotoLine` (the `— NO PHOTO: …` suffix) is deleted with its five callers; `qpNoPhoto` / `problemNoPhoto` state, `makeSetCiNoPhoto` and the three input blocks are removed (a stray `setQpNoPhoto("")` in the quick-report reset threw `is not defined` AFTER filing — the harness caught it; grep case-insensitively for `nophoto` when removing state). What remains is a plain flag once they try to send: `.proofRow.proofMissing` "📷 Add a photo of the problem *" (portal EN/ES: "Agrega una foto del problema"), the checklist hint `⚠ Add a photo of the problem — required`, and the pre-submit modal line `needs a photo`. Old reports keep their `ci.noPhotoReason` / `NO PHOTO:` text; nothing reads them. Harness `train/v454.mjs` (portal + quick report): no excuse text/input before or after the block, blocked with an action but no photo, accepted after `setInputFiles`, no `NO PHOTO` on screen. NOTE: in local mode `saveOneInspection` does NOT write `sdx_history_cache_default` (that key is only the online warm cache) — assert on the UI (flash, Submitted! screen), not on that key. The checklist panel still cannot be driven headlessly (v439/v445/v450); its change is grep-verified (0 hits for the old strings in `dist/`).
- v453: follow-ups by date. Joxel: "i want the issues that are corrected or not to be organize by date" (screenshot of the ✅ Fixed list, one long undated run). Open items: a fifth grouping button `📅 By Date` (`fuGroupBy === "date"` → `fuDateGroups` via the shared `groupOf`, keyed by `dateKeyOf(f)` = `f.dateStr` or the day of `f.ts`, newest first, header `📅 Today · Thu, Sep 17, 2026 · N stands` through `dayLabel`; the Type / Floor pills still narrow it). Fixed items: the ✅ Fixed panel is grouped by the day the crew closed it (`fixedDays`, same shape as the v438 `cleanDays`; `.fuFixedDay` header with the count and who closed them). Also fixed in passing: `remindGroup` built the title from `g.loc`, which is undefined for the v447/v449 Type and Floor groups ("🔁 Rechecks Needed — undefined") — it now names the group by mode (`head`/`headNice`) and prefixes each bullet with the stand when the group is not a venue. Harness `train/v453.mjs`: a cleaning crew closes one item in the same context (then Lock App), the inspector opens Follow-ups → By Date and it asserts every header is a date, newest first, the items sit inside, the reminder title has no "undefined", and the Fixed panel shows the row under `📅 Today`.
- v452: the calendar month title that never changed. Joxel: "when im moving to next date the month does not change" — "the days change, the title doesn't". `MonthCalendar` (22480) is internally consistent, so the state WAS advancing: the bug is React + Google Translate. The translator REPLACES a text node with its own `<font>` wrapper, leaving React holding a detached node, so its in-place `nodeValue` write goes nowhere and the title freezes — while the day cells, keyed by date (`key={d}`), are rebuilt as new DOM and keep moving. (His screenshot's weekday row reading `S M T IN T F S` is the fingerprint: "W" translated.) Fix: the label is now `<span key={month}>{monthLabel}</span>` so React remounts the node instead of patching it, and the header row, the weekday row and the day numbers carry `notranslate` / `translate="no"` (same treatment as v432). Both call sites — `SchedulePage` (22599) and the admin calendar (22947) — pick it up with no prop change. Harness `train/v452.mjs` steps ▶×3 / ◀×5 cross-checking the title against the day count and the weekday the 1st lands on, then simulates the translator by replacing the label's text node with a `<font>` holding a COPY (detaching the original — wrapping the original node in place does NOT reproduce it, React still finds it) and asserts the title still advances. Proven both ways: on the v451 build that assertion fails ("July 2026" -> "July 2026"), on v452 it passes. GENERAL LESSON: anywhere the app updates a stable text node in place, the translator can freeze it — key the node or mark it notranslate.
- v451: the crew's ✅ Fixed box, fixed. Joxel: "i do not want them explaining themselves why they did not put any pictures", "de quien es el desorden we already know if it is subcontractor or concession", "the crews do not have the option to upload pictures", "for the guide inspection after they put certain amount of information it should autosave". (a) ROOT CAUSE of the excuses: the photo labels live in `.crewActions`, the ELSE branch of the `action?.key === f.key` ternary — so tapping ✅ Fixed **removed the photo buttons from the screen** while the gate demanded a photo. Both `📷 Take photo` (capture) and a NEW `🖼 Upload` (gallery, no `capture`) now render INSIDE the action box (plus on the card), all through `T(en,es,ht)`. (b) The v439 no-photo excuse input is deleted — `noPhoto` was written into the status entry and read nowhere. (c) The v437 "whose mess" question is gone; `commit` derives `whose` from `f.locType` (`Subcontractor` / `Portable - Subcontractor` → `"sub"`, anything else → `"ours"`, unknown → unset) so all ten readers — the `🧾 N to bill` chip, BILL THE STAND, the cleaning log, `exportCleaningReport`, the Excel column, the Timeline — keep working untouched. (d) Draft autosave: the old effect was `setInterval(30000)` with every form field in its dep array, so **the clock restarted on every keystroke and a steady typist never saved**; it is now a 4 s debounce (`setTimeout`), and the draft also goes to Firestore `sharedMemory/inspectionDrafts` keyed by badge hash (`saveDraftCloud` / `loadDraftCloud` / `clearDraftCloud` + `slimDraft` strips `photos`/`itemPhotos`/`notesPhotos`), cleared on save, offered by the restore banner when no local draft exists. The strip reads "Draft saved just now · ☁ in the cloud". No rules change needed — `sharedMemory/{docId}` already covers it. Harness `train/v451.mjs` proves the box has no whose-row and no excuse input, Send is blocked with no photo and enables after a GALLERY upload, the stored entry is `{how:"crew", whose:"ours"}` with no `noPhoto`, and that the draft is absent while typing then present after the debounce. The cloud copy cannot be tested in local mode (FIREBASE_ON false).
- v450: the flagged-checklist panel lost its duplicate chip block. Joxel (screenshot of WHAT / WHAT'S WRONG / WHERE stacked above 📍 SPECIFIC LOCATION): "can we get rid off those many options and just keep it like it was before" — WHERE and SPECIFIC LOCATION asked the same question twice. Removed `<SpecificsPicker>` and its `makeSetSpec` setter from the fail panel ONLY (he chose: keep SPECIFIC LOCATION, inspection form only). The description's red border / hint no longer depends on `isSpecific(… ci.specDetails …)` — which would have lit up permanently with no chips to tap — and now just asks for 4+ words. `ci.specDetails` stays in the data model (old reports carry it; the section-level 🎯 picker at 24432/24739 still writes it). Untouched: SPECIFIC LOCATION, CORRECTIVE ACTION, the v439 photo gate, and the chip block in the stand portal + quick report (portal verified still rendering one `.specPicker`). NOTE FOR NEXT TIME: driving a flagged checklist item from the harness has now failed three times (v439, v445, v450). The item cards read "Tap to fill in" and clicking the card div, the section tab, or the STRUCTURE/SINKS chips does NOT render `.clBtnFail` — whatever expands an item card is still unidentified, so the fail panel ships eyeball-verified only.
- v449: By Floor, and the chip wall is gone. Joxel: "can i also have the option to separate them by floor" + "i want this to look better" (screenshot: 14 filter pills over two lines, grouping toggle crammed on the right, and `PORTABLE 6` shown TWICE because `fuTypeCounts` keyed the raw `locType` while several raw values render the same PORTABLE badge). Now: the ten stand-type pills are two compact `.fuFilterPill`s — `🏷 Type: all ▾` / `🏢 Floor: all ▾` — each opening the existing `.fuTypeMenu` with counts (`SUBCONTRACTOR · 51`), type counts keyed on `standTypeBadge(t).short` so each badge appears once (empty = `NO TYPE`), and an ✕ to clear. A shared `groupOf(keyFn, rankFn)` builds both `fuTypeGroups` and the new `fuFloorGroups`; `fuGroupBy` gains `"floor"` with a `🏢 By Floor` button (order Floor 1→2→3→Ground→other→No floor, header `🏢 Floor 1 · 3 stands`). `fuTypePick` and `fuFloorPick` both narrow `fuVisible` and each other's group lists, so Subcontractor + Floor 2 is exactly that slice in every grouping. `.fuFilterBar` puts the toggle on its own line under 720 px. Harness `train/v449.mjs` — note when driving it: the filter menus are toggles, so close any open menu before opening another, and expand only groups that have no `.fuGroupBody` or you collapse the ones already open.
- v448: the stand type is STAMPED when the problem is filed, not guessed later. v447 derived it at display time from `standSeeds()`, which is wrong whenever a report name does not match the license sheet. Now `computeFollowups` records `catLastSeen[key].locType = rec.locationType` from the newest record that flagged the issue and returns it, and the panel's `typeOf(f)` returns the stamped value first, falling back to the registry match only when the record carries none (so old reports still resolve). The quick report — the one path that stored NO `locationType` — now detects the type from the unit/name as you type (`detectStandType`, same unit + `sameStandName` match), shows it as a `<StandType>` badge in a "Type of stand" row with a select to correct it (`qpType` / `qpTypeAuto`), and writes it to `rec.locationType`. The type now also shows on the crew card (`renderCrewItem`, marked `notranslate`), on the Timeline fix event, and in the subcontractor cleaning PDF header next to the license (`License NOS… · SUBCONTRACTOR · 3 cleans`). Harness `train/v448.mjs`: typing unit 332 auto-detects SUBCONTRACTOR, overriding to Portable and filing flips the follow-up card and the type chips to PORTABLE — proof the stamp beats the registry. The crew-card badge ships unverified by the harness (the local crew board came up empty in that seed).
- v447: follow-ups split by stand type. Nothing in the follow-up pipeline knew a stand's type (`computeFollowups` keys on `loc::cat` and returns no `locType`). The panel's analysis memo now resolves it from `standSeeds()` — the license registry IS the stand list — indexed by `normUnit(unit)`, matched with `sameStandName`, falling back to the unit's only stand and then to the newest report's `locationType`; every follow-up and every group item carries `f.locType`. UI: a third grouping button `🏷 By Type` (`fuGroupBy === "type"` → `fuTypeGroups`, ordered Subcontractor → Portable·Sub → Portable → Concession → Kitchen → Bar → Other → Unknown, header = `<StandType>` badge + stand count, Remind Team / Resolve all unchanged since they only need `g.items`), one filter chip per type present (`.fuTypeFilter`, `fuTypePick` narrows `fuVisible` in EVERY grouping), and a `<StandType>` badge on each card. Exports: a `Stand Type` column after Floor in `exportSelectedFollowups` (every later column index shifted by one — widths, header, addRow, autoFilter to col 21, merged title to U, and the `col ===` style checks) and in `exportBeforeAfter` (photos now placed at cols 16/17), plus a type line under the stand in the PDF/Word table. Harness `train/v447.mjs` seeds problems at a Subcontractor and three Concession stands: asserts the chips, the badges, the By Type groups, that the SUBCONTRACTOR chip narrows to 1 item, and reads the exported workbook back with exceljs to confirm `Stand Type` lands in column E with nothing else shifted.
- v446: "is not loading" — Past Reports showed the generic "No inspections saved yet" empty state even when the read had FAILED, because `loadHistory` swallowed every error (`catch { return { list: [] } }`). Now it logs and returns `{ error }`, the History page keeps `historyError`, and on a failed read it re-seeds from `sdx_history_cache_<venue>` instead of wiping the screen. Three distinct empty states: "⚠️ Could not load your reports — nothing was lost (code)" with a ↻ Try again button (`retryHistory()`, `fresh: true`), "Loading your reports…" while `!historyLoaded`, and the real "No inspections saved yet" with a ↻ Reload plus a `Venue: <id> · <SDX_VERSION>` line so the next screenshot says which venue was queried. Investigation notes for next time: `venueRegistry` maps **Hard Rock Stadium → venue id "default"**, so the Global-Admin "Viewing reports for" banner is still the legacy `inspections` collection (confirmed via REST: 8+ docs, all carrying an `id` field, so `mergeHistory` was not dropping them); the live site is NOT reachable from this sandbox (`ERR_TUNNEL_CONNECTION_FAILED` on joxelds.github.io), so cloud-only failures cannot be reproduced here — the error branch ships unverified by the harness, which only proves the normal path still renders.
- v445: temperature follow-ups name the unit. ROOT CAUSE: `buildActionItems` wrote the equipment name to `item.area` (`etArea`) but `computeFollowups` keys and titles off the issue text only (`cat = issue.split(":")[0]`) and never read `area` — so the card could only say "Temperature out of range" AND every out-of-range unit at a stand collapsed into ONE follow-up (`LOC::Temperature out of range`), meaning three coolers looked like one problem and clearing it cleared all three. Now `collectEquipTemps` also carries `assetTag`/`brand` off the node, and both temp branches write `Temperature out of range — WALK-IN COOLER (DELFIELD · BACK OF HOUSE · SDX-CL-114-1): 50°F (max 41°F…)`, so the category is per-unit: separate card, separate recheck, separate crew task, and the name rides into every export and the Timeline for free. For reports saved BEFORE this, `computeFollowups` now carries `item.area` through as `f.area` and both the follow-up card and the crew card show a `.fuEquipChip` (🧊 WALK-IN COOLER — BACK OF HOUSE) when the category does not already contain it. Open temp follow-ups keep their old generic key until cleared, so a stale generic card can sit beside a new named one for one recheck cycle. Verified two ways: the generator block was extracted verbatim from `src/App.jsx` and run in node (two distinct named categories), and `train/v445.mjs` proves the UI renders three separate named cards and that resolving one leaves the others open.
- v444: Past Reports stopped collapsing back to 30. ROOT CAUSE: the `visibilitychange` effect on the History page refetched `pageSize: 30` and called `setHistory(list)` — a full REPLACE — so every app-switch, photo or screen-lock wiped the loaded pages plus anything that lived only in memory (Search-all matches, imported reports); and `prefetchedRef` never reset, so the auto second page never fired again. Now: a `historyRef` + `mergeHistory(prev, incoming)` (by id, `_pinned` records always kept, sorted by `savedAt` desc) is used by the first page, the visibility refresh, load-more and Search-all; the refresh is skipped when the last load was < 60 s ago, fetches `Math.max(100, current length)` so the depth survives, and only advances `historyLastDoc` when it went at least as deep; `prefetchedRef` resets on every fresh page. First page 30→100, load-more 100→200, and the warm cache seeds the WHOLE cached array instead of `slice(0, 30)`. Search-all results are tagged `_pinned` so nothing can drop them. New `.histDepthBar` above the list — "Showing 50 of 140 reports loaded · more on the server", an "updated HH:MM" stamp and a `⬇ Load all` button (`loadAllHistory()`, pageSize 2000, merged). ANALYTICS now reads a separate deep list: opening the Analytics tab triggers one `loadHistory({pageSize:2000})` into `analyticsHistory` (served by the 60 s `_speedCache`) and all six `filtered.length > 0 ? filtered : history` props became `… : (analyticsHistory.length ? analyticsHistory : history)`, so Follow-ups / Insights / Predictive / Timeline see every stand, not the first page. Harness `train/v444.mjs` seeds 140 reports, fires a real hidden→visible cycle (row count KEPT), taps Load all (141 rows) and asserts an issue that only exists in report #130 shows up in Follow-ups.
- v443: a "📸 Before & After" Excel of the jobs he picks (Joxel: "when they complete a task i can select and download to see before and after and start sending" — format "like a excel"). `exportBeforeAfter(items)` in `RecurringIssuesPanel` writes one row per job — # / Stand / Unit / Floor / License (from `licenseRows()` by `normUnit`) / Problem / What was wrong / Flagged / Fixed by / Date solved / Time solved / Min on job / Whose mess (amber for STAND / SUBCONTRACTOR) / Corrective action + comments — and **embeds the pictures** in the last two columns (up to 3 each; before = report photos + untagged follow-up photos, after = `tag === "after"`). Images go through the existing `fetchAsDataUrl` (Firebase SDK `getBlob` first, which is what beats Storage CORS) then `wb.addImage` + `ws.addImage({ tl, ext: 108×116, editAs: "oneCell" })`, row height 92; a photo that cannot be fetched leaves a blue hyperlink in the cell instead of vanishing, and empty cells read "no before/after photo". Buttons: next to Excel / PDF / Word in select mode (uses `selPicked()`), plus a shortcut in the ✅ Fixed and 🧹 cleaning panel heads for the whole visible list; `baBusy` shows `Preparing… n/N` and disables them while photos download. Harness `train/v443.mjs` captures the workbook via the `URL.createObjectURL` stub and asserts `ws.getImages().length === 2` — the pictures are really in the file.
- v442: fixes appear on the Activity Timeline. The `analyticsTab === "timeline"` builder now pushes a third event type from `venueSettings.followupStatus` — every entry with `status === "resolved"` and a `ts` — resolving stand / unit / category / detail against `computeFollowups(src, {...vs, followupCleared:{}})` so cleared items still resolve (falling back to splitting the `loc::cat` key). Green dot, green card, `✅ STAND #unit — Category`, a FIXED chip, `N min on the job` when `mins` is known, an amber `BILL THE STAND` chip when `whose === "sub"`, and a sub-line with who closed it, the solve time, `started HH:MM` and "was already clean" where it applies. Sorted into the same list as inspections and HACCP submissions. Harness `train/v442.mjs` (crew fix then inspector in the SAME context via `lockApp` — a second `mkCtx` has its own storage and never sees the write).
- v441: print a whole floor at once. The posters & licenses tab (`KitchenQrPage`, the select bar next to "Select All") now renders one pill per floor present in the currently shown list — `🏢 Floor 1 (46)` / `Floor 2 (16)` / `Floor 3 (40)` / `No floor` — built from `k.floor` on `shown`, sorted by the first number in the name (No floor last), hidden when there is only one floor. Tapping adds that floor's ids to `selectedIds`, tapping again (the pill reads `✓ Floor 1`) removes only those ids, so floors combine (Floor 1 + Floor 2 = 62 selected) and the Print button count follows. Harness `train/v441.mjs`.
- v440: select anything the team did, and Resolve actually sticks. (a) A single `selPool` (open `fuVisible` + `fixedList` + `cleanAll`, deduped by key, the fixed copy winning because it carries `fixed*`) feeds all three export buttons through `selPicked()`; the checkbox became a shared `selBox(f)` (`.fuSelBox/.fuSelMark`) rendered on open rows, ✅ Fixed rows AND cleaning-log rows, and "Select all" ticks whichever panel is open. `exportSelectedFollowups` gained six columns — Fixed by / Date solved / Time solved / Started / Min on job / Whose mess (billable rows amber) — the 13-widths-vs-14-headers off-by-one is fixed, autoFilter and the merged title run to column T, and resolved rows now read "Fixed" (or "Fixed — then flagged again"); PDF/Word gained a Solved column. (b) Resolve: the crew `commit` and `commitCorrectives` now stamp `followupCleared` with `Math.max(ts, (f.ts||0)+1)` like `markResolved` (a raw `Date.now()` could lose to a date-only report ts, so the item never left the list); `clearedLocal` is persisted to `sdx_fu_cleared_<venue>` and seeded from it (the panel unmounts on every Analytics tab change and used to drop an unlanded clear); `saveVenueSettingsMap` retries once after 4 s and then fires `sdx-save-failed`, which the panel shows as `.fuSaveWarn`; a `.fuUndoStrip` proves the clear with the time and a ↩ Undo for 9 s; `fixedList` marks `reopened` when `clearedTs < f.ts` and shows a red REOPENED chip instead of listing the item as a clean fix while it is also open. An item still returns when a NEW report flags the same `loc::cat` — by design. Harness `train/v440.mjs` (workbook captured by stubbing `URL.createObjectURL`, then read back with exceljs).
- v439: a photo AND a corrective action are mandatory everywhere a problem is flagged. New shared helper `proofGate({photos, action, noPhotoReason})` next to `isSpecific` (a photo, OR a typed reason ≥ 8 chars why there is none, plus an action ≥ 5 chars) and `noPhotoLine(reason)` which appends `— NO PHOTO: …` to the issue text so the excuse rides into every existing export with no schema change. Wired into: the stand QR portal (`handleSubmit`, two new `L(en,es)` fields under the photo button), the Follow-ups quick report (`submitQuickProblem`; the photo label now says "required"), every checklist item marked NO (`ci.corrective` now starred + red, new `ci.noPhotoReason` via `makeSetCiNoPhoto`) gated in `onTransform` with `preSubmit: { hard: true, proof: true }` so the "continue anyway" button never renders (the old soft "issues without a photo" line was removed), and the crew ✅ Fixed action box (an after photo or a typed reason, stored as `noPhoto` on the status entry). Both new fields write into `actionItems[0].corrective` and `notes` ("Corrective action: …"). Also removed the duplicate 💾 Save in the sticky bottom bar (the red "Save Report" in the header already saves; a ✅ Saved chip remains). Harness `train/v439.mjs` proves blocked→accepted for the portal, the quick report and the crew board, and that the hard modal has no bypass. NOTE: crew flows need their OWN context (a signed-in inspector in a shared context swallows the badge login), and driving a flagged checklist item from the harness was not solved — the fail panel only renders inside an expanded section.
- v438: the clock, not a chip. Joxel: "I just want to know at what time the problem was solved so I can track their timeline" + "remember all the times my crew did today, or at least all the places". The "How long did it take?" chips from v437 are gone; `commit` now reads the crew's own “In process” entry (`prevSt.status === "in_progress"`) as `startTs` and computes `mins` from it, storing both on the resolved entry — only "whose mess" is still asked. Every cleaning-log row carries a `.fuTimeline` (🚩 flagged · 🔧 started HH:MM · ✅ solved HH:MM · N min on the job), the Fixed panel shows the exact clock time next to "x min ago", and the PDF has Date / Started / Solved / Time-on-job columns. The log is grouped by day (`cleanDays`) with a `TODAY · N cleans · N stands · time · who` header and the list of stands under it, plus Today / Last 7 days / All range chips (default Today). If a crew taps ✅ Fixed without ever tapping 🔧 In process there is no start time — the row then shows only the solved time. Harness `train/v438.mjs`. NOTE: rewriting `sdx_venue_settings_default` from `page.evaluate` throws "Illegal invocation" in this app — drive the real buttons instead.
- v437: cleaning work is tracked and billable. When a cleaning crew taps ✅ Fixed and answers "We cleaned it", two more chip rows appear — **whose mess** (🏟️ Ours / 🧾 The stand’s · subcontractor) and **how long** (15/30/45/60/90/120 min); both are required before Send (`commit(f, kind, note, how, whose, mins)` stores `whose`/`mins` in the `followupStatus` entry and names them in the crew comment). Inspector side: a `🧹 N cleans · N to bill` chip in the Follow-ups header opens `.fuCleanPanel` — totals (jobs, crew hours), hours per crew member, a "billable only" filter and every job with before/after thumbnails (after photos outlined green). `exportCleaningReport(items)` prints (via `printHtml`, no pop-up) a landscape PDF grouped by stand with the license number from `licenseRows()`, what was cleaned, who cleaned it, time, whose mess and the before/after photos — the sheet to send the subcontractor. Harness `train/v437.mjs` drives crew → inspector → report.
- v436: the per-item `🔔 Remind` in Follow-ups is now a basket, not a send. `remindTeam(f)` became `remindMany(items, flashKey)` (one announcement, one bullet line per item, sorted by stand then category; single item keeps the old wording) and the button toggles `remindPick` (green `✓ Added` via `.fuBtnRemind.on`). A `.fuRemindBar` above the list names the picked items and sends them all as one Quick Check with one `notifyAssignedUsers` call. The per-stand "Remind Team" button (`remindGroup`) is unchanged. Harness `train/v436.mjs` asserts three taps post nothing until Send, then exactly one announcement with three bullets.
- v435: real push for fixes — `functions/index.js` (`notifyOnFix`, Firestore v2 trigger on `venues/{venueId}/sharedMemory/venueSettings`) diffs `followupStatus`, ignores replays older than 10 min, and multicasts one FCM message per newly resolved key to the tokens in `sharedMemory/pushTokens` (only roles inspector/admin/global_admin/location_manager), pruning dead tokens. Client: `public/firebase-messaging-sw.js` (compat SDK 10.14.1, background handler + notificationclick focus), `registerPushToken()` / `onPushMessage()` in `src/firebase.js` (called on sign-in for watcher roles; foreground messages go to the same bell via `fireNotification`). `VAPID_PUBLIC_KEY` in `src/firebase.js` is EMPTY — Joxel pastes the Web Push key and runs `npx firebase deploy --only functions,firestore:rules`; steps in `docs/PUSH-SETUP.md`. Blaze plan required (free tier covers this volume); iOS only delivers to a Home Screen PWA. Push delivery cannot be tested from this sandbox — the harness only proves the app still builds, registers without throwing when the key is empty (returns `not-configured`), and that the v434 in-app path still fires.
- v434: the inspector hears about fixes. ROOT CAUSE found: the `inspectorNotifications` collection was **missing from `firestore.rules`**, so the deny-all catch-all rejected every `saveInspectorNotification` write (live collection had 0 docs) — assignment notifications were dead too. Added a rules block for it (needs `firebase deploy --only firestore:rules` from Joxel's machine to take effect) and lowercased the two writers that wrote raw display names (the reader queries lowercase); crew updates now also go to every admin / global_admin, not just `f.inspector`. The feature itself does NOT depend on that deploy: crews already write the fix into `venueSettings.followupStatus`, which every device receives through the existing `onSnapshot`, so a new effect in the app shell (`FIXED_SEEN_KEY` = `sdx_fixed_seen_<venue>`) fires `fireNotification("✅ Fixed — STAND #unit", "category · by NAME")` for any resolved entry newer than the stored baseline; the baseline starts at `Date.now()` on first run so the backlog stays out of the bell. Follow-ups header gained a clickable `✅ N fixed today` chip opening `.fuFixedPanel`, rebuilt with `computeFollowups(history, {...vs, followupCleared:{}})` because the normal list drops cleared items. Cleaning crews are asked how it was resolved when they tap ✅ Fixed (`.crewHowRow`: "We cleaned it" / "It was already clean", required for that role only); the answer rides in `followupStatus[key].how` and shows in the alert and the Fixed panel. No push server exists, so nothing arrives with the app fully closed — say so.
- v433: the "Be specific" chip block is visually quieter (Joxel: "make it look a little less charged, it's going to confuse the user" — he first asked to delete it, then kept it). Same behaviour and same required rows everywhere; only `.spec*` in App.css changed: hairline border, `max-width: 620px` so it stops spanning a wide form, head 600-weight muted and reworded to "Tap what applies" / "Toca lo que aplica" (no 🎯, no "Be specific"), labels lighter, chips 1px / 600 weight with the selected state still solid navy, missing rows amber instead of a red block. NOTE: `.haccpCard` is a WHITE card — white-alpha overrides make the picker and its chips invisible there, so the portal override uses light greys (`#f7f8fa` / `#eceef3`).
- v432: the issue text on crew cards translates too. Picking a crew language now calls `applyLanguage(code)` from `LanguageFab.jsx` (so the page translator runs on the report content, Kreyòl = Google `ht`) instead of clearing the googtrans cookie, and every node whose words come from `T()` is marked `className="... notranslate" translate="no"` (header, lang row, tabs, stats, pick row, chips, empty states, stand/floor heads, item meta, move row, before/after heads, photo hints, actions, action box, status stamp, stand picker) so Google never re-translates the native strings or stand names. Left translatable on purpose: `.crewItemTitle`, `.crewItemDetail`, `.crewReportIssue`. Also fixed the two strings v431 missed (`⏰ overdue`, `✋ type by X`). Free translate APIs are blocked from this sandbox (googleapis returns a block page, MyMemory 403), so the widget path is the only verifiable one — the harness checks the cookie, the saved language and which nodes are protected; real translated content is confirmed on a phone.
- v431: crew boards speak English / Español / Kreyòl natively. `CrewBoardPage` holds `cl` (localStorage `sdx_crew_lang`, falling back to `sdx_portal_lang` then the `googtrans` cookie) and `T(en, es, ht)`, the same idea as the portal's `L(en, es)`; picking a language clears the googtrans cookie so Google never double-translates. Three pills + a `🌐 +` button (fires `sdx-open-lang` for the other 17 languages) sit above the tabs (`.crewLangRow/.crewLangBtn` in App.css). Board titles/nouns come from `boardTitle`/`boardNoun`, not `CREW_META`, so every header, menu item, tab, stat, filter, empty state, card button, action box and flash is translated. Report content (problem text, stand names, issue categories) stays as written.
- v430: the P122 Arepa Cart / Sub (NOS2334419) moved to unit 119 per the 2026 sheet note — `LICENSE_REGISTRY` row unit 122 → 119 (note "Moved from 122"), plus P119 keys in `HARD_ROCK_LICENSE_SEED`. Stand id went from `u:122~arepacartsub` to `u:119~arepacartsub`; checked first that no equipment / verified entry referenced the old id and that the `licenseRegistry` overlay had no 122 key. The full 2026 sheet (89 active + 17 requested/needed) was diffed against `LICENSE_REGISTRY` and matches row for row; only deliberate spelling fixes differ (Little Caesar, Benihana, Chef Creole, Donuts, Shula Burger, Mr. Chory).
- v429: PDF exports print through a hidden iframe (`printHtml(html, fallbackName)` next to `downloadBlob`) instead of `window.open` — Safari blocked the pop-up because `exportBulkPdf` awaits HACCP loads before opening it; fallback downloads the HTML. Follow-ups tab: "Select to export" → Excel / PDF / Word (`exportFollowupsDoc(items, "pdf"|"word")` in RecurringIssuesPanel, same rows as the Excel export, landscape table, photos in the PDF). Bulk export bar (`.bulkBar`) wraps on phones. Harness seed has no flagged issues, so the bulk issue picker can't be driven locally — test PDF printing via the follow-ups export.
- v428: explainer v5.2 (white logo, 8× recording; media `91f4832b-b620-4ff8-a7cf-6a95f4fcfe1b`) lives on the marketing page too: `public/marketing/video/sdx-explainer.mp4` (1280×720 30 fps CRF 26, 13 MB) + poster jpg, section `#video` after the hero. Playwright's Chromium can't decode H.264, so the harness only checks the element/poster. Request-access form: Department select removed (role tile sets it; 'Office / other' still asks).
- v427: crew / History dropdown menus anchor to the fixed header (a `position:relative` wrapper + the mobile `left:0` rule pushed them off-screen and widened the page); invite join with an existing badge now says "already exists as ROLE" instead of signing into the old account; Ecolab invites get the Ecolab department. Invite tokens live in `venueSettings.inviteTokens` (verified on the server via the Firestore REST API — readable from here).
- v424: Admin → Invite links has a QR button per role (modal with the code, Save PNG / Print / Copy link); uses the lazy `getQRCode()` loader.
- v423: typing a site name threw `invLicenseData is not defined` in `applySiteAutofill` (map only exists in the analytics panel); guarded.
- Sales video scripts (2026-09-21, Joxel: "I think I'm going to start making videos about the system to sell it" → chose scripts + shot list): `docs/training/sales-video-scripts.md` — positioning block (pitch / 3 pains / 3 proofs / ask), a 90 s pitch (12 shots, ~237 words), five 30–45 s feature videos (QR poster, written-in-their-words reports, before/after, who logged today, the inspector's walk), recording checklist (demo data, fake 305 555 phones, no badge codes, no Sodexo/Hard Rock logos when selling to other venues) and 15 s EN/ES social cuts. Every shot row = on screen (screens/NN-*.png or live flow) · EN line · ES line. Docs only, no SW bump.
- Feature handbook by department (2026-09-21, Joxel: "Resume of all of the system features to train people and each department"): `docs/training/system-features-by-department.md` — one page for the whole system + rules for everyone, then Inspector (form, History, Follow-ups, Temps tracker, Stands & equipment, Messaging), Stand teams (portal), Maintenance, Cleaning, Ecolab, Admin; every row EN | ES with the in-app path and the `screens/NN-*.png`; printable "your 5 taps" cards per department and a 20-minute training plan per department (demo / hands-on / check). Covers v434–v472. Docs only.
- Training deck (2026-09-22, Joxel: "i need to give a training of this"): `docs/training/SDX-Inspect-Training.pptx` (+ `.pdf`), 43 slides 16:9 built by scratchpad `train/deck.mjs` (pptxgenjs installed in the scratchpad, NOT in the repo): title, agenda, the loop (7 boxes), devices, rules; sections with a navy divider per department — Inspector 10, Stand teams 6, Maintenance 5, Cleaning 2, Ecolab 2, Admin 4 — then four quick-reference card slides (EN | ES) and the 20-minute session table. Layouts P1 (one phone right, title w 6.8), P2 (two phones at y 1.6), W1 (landscape image), T2 (EN | ES panels), SEC; every slide has EN + ES speaker notes. Screens from `docs/training/screens/` + `train/v473/card-390.png`. QA: LibreOffice here ships only `libreoffice-core` — `apt-get install libreoffice-impress libreoffice-writer poppler-utils` (needs the sandbox off) before `soffice --convert-to pdf` works; `pdftoppm -r 60` + a Pillow contact sheet to eyeball. Fixed on review: single-phone titles ran under the phone, two-phone slides touched the title, device stack overlapped, one two-line title.
- Training deck v2 (2026-09-22, Joxel sent his own 10-slide `inspection_system.pdf`: "I did this presentation do the design like this"): the 43-slide deck rebuilt in HIS look by scratchpad `train/deck2.mjs` (same content/notes as `deck.mjs`; helpers swapped): white slides with a light-blue glow and royal-blue sweeps (`train/deckassets/bg-glow|wave|band|half|title.jpg`, rendered from HTML/SVG by `train/assets.mjs` with Playwright; blue sampled from his PDF = `2A3492`), the Sodexo lockup `logo.png` (official SVG + "INSPECTION SYSTEM" in Barlow Condensed 800 italic, `train/fonts/BarlowCondensed-ExtraBoldItalic.ttf` from Google Fonts — the stand-in for his Zing Rust), headlines in `Arial Narrow` bold italic UPPERCASE blue, bullets in white rounded cards with `shadow`, phones/iPads tilted (`rotate` ±4–12°) with drop shadows and fanned in pairs, section dividers on `bg-half` with two phones over the blue, line icons (`icon-*.png`, blue SVG) in the top-right corner of a few slides. Rules learned: keep the blue shapes BELOW the headline band (a top-right wave put blue under blue text); tilted images need ~0.3 in of slack from the right edge; headline width shrinks to 6.0 when a corner icon is present (`_slideObjects` patch in `P1i/W1i/T2i`). Output overwrote `docs/training/SDX-Inspect-Training.pptx/.pdf` (17 MB / 7 MB). Closing slide = his slide 10: two real QR codes decoded from his PDF with OpenCV (`train/deckassets/qr-system.png` → `https://joxelds.github.io/Claude/`, `qr-video.png` → the Higgsfield cloudfront mp4 of explainer v5.2 `91f4832b…`, generated with the app's `node_modules/qrcode`), labelled SYSTEM QR CODE / VIDEO GUIDE with ES sub-labels; the rendered slide decodes back to both links. NOTE: the video link is a CDN URL that can expire — the same file is served at `joxelds.github.io/Claude/marketing/video/sdx-explainer.mp4`.
- Promo (60 s, no voice): prompt pack in `docs/training/promo-prompt-pack.md`; 8 Seedance shots + 3 phone clips (`train/phone.mjs`, app fills top-left 390×780 of the 780×1560 recording → crop) composed by `train/gen_promo.py` (concat, rounded phone overlays, drawtext Montserrat, synthesized 100 bpm bed). Local ffmpeg-static has no drawtext: validate locally without text, render in the sandbox. Result media `c720486d-3fe9-478b-93b0-24cdaa2ecad6`.

## Open items
- Compact `equipmentRegistry` (size risk). Audit workbook was delivered 2026-09-14 (29 orphan units, 28 duplicate copies, 41 units missing brand/location, 43 stands without equipment, 22 stands without license, verify walk 0/116).
- Domain change: waiting for the domain name (needs CNAME, vite `base`, SW scope, Firebase authorized domains).
- App Store 3.2.0 resubmission on hold; Refero token exposed earlier — rotate; Higgsfield connector never got a callback URL.
